local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local StarterGui = game:GetService("StarterGui")
local UserInputService = game:GetService("UserInputService")
local Camera = Workspace.CurrentCamera
local lp = Players.LocalPlayer
local PlayerGui = lp:WaitForChild("PlayerGui")
local VirtualInputManager = game:GetService("VirtualInputManager")
local TweenService = game:GetService("TweenService")

-- Remotes with error handling
local testRemote = nil
local success, result = pcall(function()
    return ReplicatedStorage:WaitForChild("Modules"):WaitForChild("Network"):WaitForChild("Network"):WaitForChild("RemoteEvent")
end)
if success then
    testRemote = result
else
    success, result = pcall(function()
        return ReplicatedStorage:FindFirstChild("Modules"):FindFirstChild("Network"):WaitForChild("RemoteEvent")
    end)
    if success then
        testRemote = result
    else
        testRemote = ReplicatedStorage:FindFirstChild("RemoteEvent") or Instance.new("RemoteEvent")
    end
end

-- Killers and Survivors folders
local KillersFolder = nil
local SurvivorsFolder = nil

local playersFolder = Workspace:FindFirstChild("Players")
if playersFolder then
    KillersFolder = playersFolder:FindFirstChild("Killers") or Instance.new("Folder")
    KillersFolder.Name = "Killers"
    SurvivorsFolder = playersFolder:FindFirstChild("Survivors") or Instance.new("Folder")
    SurvivorsFolder.Name = "Survivors"
else
    KillersFolder = Instance.new("Folder")
    KillersFolder.Name = "Killers"
    SurvivorsFolder = Instance.new("Folder")
    SurvivorsFolder.Name = "Survivors"
end

-- ===== SOUND IDs FOR AUTO BLOCK =====
local autoBlockTriggerSounds = {
    ["116468089135195"] = true,
    ["74809026448465"] = true,
    ["106836941416453"] = true,
    ["98111231282218"] = true,
    ["124903763333174"] = true,
    ["127557531826290"] = true,
    ["105204810054381"] = true,
    ["74842815979546"] = true,
    ["108651070773439"] = true,
    ["131123355704017"] = true,
    ["128856426573270"] = true,
    ["128367348686124"] = true,
    ["104910828105172"] = true,
    ["140242176732868"] = true,
    ["115026634746636"] = true,
    ["84116622032112"] = true,
    ["108907358619313"] = true,
    ["127793641088496"] = true,
    ["86174610237192"] = true,
    ["95079963655241"] = true,
    ["101199185291628"] = true,
    ["119942598489800"] = true,
    ["84307400688050"] = true,
    ["113037804008732"] = true,
    ["105200830849301"] = true,
    ["75330693422988"] = true,
    ["124397369810639"] = true,
    ["107444859834748"] = true,
    ["82221759983649"] = true,
    ["76467993976301"] = true,
    ["81702359653578"] = true,
    ["128195973631079"] = true,
    ["108610718831698"] = true,
    ["112395455254818"] = true,
    ["109431876587852"] = true,
    ["109348678063422"] = true,
    ["85853080745515"] = true,
    ["86785901416030"] = true,
    ["12222216"] = true,
    ["105840448036441"] = true,
    ["114742322778642"] = true,
    ["119583605486352"] = true,
    ["79980897195554"] = true,
    ["71805956520207"] = true,
    ["79391273191671"] = true,
    ["89004992452376"] = true,
    ["101553872555606"] = true,
    ["72425554233832"] = true,
    ["124903763333174"] = true,
    ["98675142200448"] = true,
    ["101698569375359"] = true,
    ["106300477136129"] = true,
    ["116581754553533"] = true,
    ["117231507259853"] = true,
    ["136323728355613"] = true,
    ["119089145505438"] = true,
    ["121954639447247"] = true,
    ["125213046326879"] = true,
    ["131406927389838"] = true,
    ["117173212095661"] = true,
    ["80516583309685"] = true,
    ["132596270805754"] = true,
    ["83851356262523"] = true,
    ["111910850942168"] = true,
    ["136728245733659"] = true,
    ["94317217837143"] = true,
}

-- ===== ANIMATION IDs FOR AUTO BLOCK (Slasher only) =====
local autoBlockTriggerAnimations = {
    ["126830014841198"] = true,
    ["109667959938617"] = true,
    ["130958529065375"] = true,
    ["94958041603347"] = true,
    ["108196996477620"] = true,
}

-- ===== GUEST1337 BLOCK/PUNCH ANIMATIONS =====
local guest1337AllAnims = {
    ["95802026624883"] = true,
    ["82036084568393"] = true,
    ["120748030255574"] = true,
    ["72722244508749"] = true,
    ["140671644163156"] = true,
    ["100926346851492"] = true,
    ["96959123077498"] = true,
    ["87259391926321"] = true,
    ["82605295530067"] = true,
    ["138040001965654"] = true,
    ["127040663332045"] = true,
    ["72007882634344"] = true,
}

-- Specific punch animations
local guest1337PunchAnims = {
    ["87259391926321"] = true,
    ["138040001965654"] = true,
    ["72007882634344"] = true,
}

-- ===== CHANCE ANIMATION IDs =====
local chanceAnimIds = {
    ["133491532453922"] = true,
    ["76649505662612"] = true,
    ["86371356500204"] = true,
    ["131189930305001"] = true,
    ["73697316043485"] = true,
    ["103301489258021"] = true,
}

-- ===== AUTO BLOCK STATE VARIABLES =====
local autoBlockForGuest = false
local autoCloneFor7n7 = false
local ragingPaceAutoParry = false
local error404AutoParry = false
local bloodHuntAutoParry = false
local ragingPaceAutoPunish = false
local onexOnexOnexOneAutoPunish = false
local blockdelay = 0
local detectionRange = 18
local messageWhenAutoBlockOn = false
local messageWhenAutoBlock = ""
local blockChance = 100
local facingCheckEnabled = true
local customFacingDot = -0.3
local killerCirclesVisible = false
local autoblocktype = "Block"
local autoSwitchBlockCharge = false
local originalBlockType = "Block"
local autoBloodOrb = false
local bloodOrbConnection = nil

-- ===== AIMBOT VARIABLES =====
local aimbotEnabled = false
local aimbotSmoothness = 0.1
local aimbotMaxDistance = 50
local aimbotFOV = 90
local aimbotUseFOV = true
local aimbotFOVCircle = nil
local aimbotCurrentTarget = nil
local aimbotConnection = nil
local aimbotActive = false
local aimbotActiveTimer = nil
local aimbotTargetCount = 0
local aimbotPredictionMultiplier = 1

-- ===== BACKSTAB MODE TOGGLE =====
local faceSameSideAsKiller = false

-- ===== PREDICTION VARIABLES =====
local BASE_PREDICTION_DISTANCE = 4
local SPHERE_SMOOTHNESS = 0.25
local MOVE_THRESHOLD = 0.15
local currentTargetSphere = nil
local currentSphereConnection = nil
local positionHistory = {}
local HISTORY_SIZE = 5

-- ===== 7N7 ROTATION VARIABLES =====
local rotationActive = false
local ROTATION_DURATION = 0.3

-- ===== SPECIAL KILLER VARIABLES =====
local isPlayingAsSpecialKiller = false
local currentKillerType = nil
local originalFacingCheckState = true

-- ===== VISUALS VARIABLES =====
local stunImmuneEnabled = false
local stunImmuneLoop = nil

-- ===== ABILITY COOLDOWN TRACKER VARIABLES =====
local cooldownTrackerEnabled = false
local activeCooldownTimers = {}
local lastGuest1337Trigger = {}
local lastChanceTrigger = {}
local GUEST1337_ANTI_SPAM_TIME = 3
local CHANCE_ANTI_SPAM_TIME = 3

-- Cooldown configuration
local SHEDLETSKY_COOLDOWN = 40
local TWOTIME_COOLDOWN = 30
local CHANCE_COOLDOWN = 45
local JANEDOE_COOLDOWN = 35
local GUEST1337_COOLDOWN = 27
local ELLIOT_COOLDOWN = 45
local DECIMAL_START = 10

-- Shedletsky sounds
local shedletskySwingSounds = {
    ["12222225"] = true,
    ["132596270805754"] = true,
    ["83851356262523"] = true,
    ["111910850942168"] = true,
    ["115267992424648"] = true,
    ["99820161736138"] = true,
    ["86710781315432"] = true,
    ["139012439429121"] = true,
    ["5569523548"] = true,
    ["92445809840331"] = true,
}

-- TwoTime sounds
local twotimeSounds = {
    ["99820161736138"] = true,
    ["86710781315432"] = true,
}

-- Chance sounds
local chanceSounds = {
    ["139012439429121"] = true,
}

-- JaneDoe sounds
local janeDoeSounds = {
    ["92445809840331"] = true,
    ["83851356262523"] = true,
}

-- Elliot throw animation
local elliotThrowAnimation = {
    ["114155003741146"] = true,
}

-- ===== DUSEKKAR SILENT AIM VARIABLES =====
local dusekkarEnabled = false
local dusekkarTargetMode = "Survivors"
local silentAimActive = false
local originalGetMousePos = nil
local mousePositionModule = nil
local currentDusekkarTarget = nil
local dusekkarActive = false
local dusekkarTimer = nil
local dusekkarPredictionMultiplier = 1

-- ===== UI REFERENCES =====
local GuestToggle = nil
local SevenNToggle = nil

-- ===== SMOOTH SHIFTLOCK MODULE =====
local SmoothShiftLock = nil
local shiftlockLoaded = false
local shiftlockWasEnabled = false

local function loadSmoothShiftLock()
    local success, module = pcall(function()
        return require(ReplicatedStorage:FindFirstChild("Systems"):FindFirstChild("Player"):FindFirstChild("Game"):FindFirstChild("SmoothShiftLock"))
    end)
    
    if not success then
        success, module = pcall(function()
            return require(ReplicatedStorage.Systems.Player.Game.SmoothShiftLock)
        end)
    end
    
    if success and module then
        SmoothShiftLock = module
        shiftlockLoaded = true
    end
end

loadSmoothShiftLock()

-- ===== SHIFTLOCK CONTROL =====
local function isShiftLockEnabled()
    if shiftlockLoaded and SmoothShiftLock then
        return SmoothShiftLock.Enabled
    else
        return UserInputService.MouseBehavior == Enum.MouseBehavior.LockCenter
    end
end

local function setShiftLock(enabled)
    if enabled then
        shiftlockWasEnabled = isShiftLockEnabled()
        
        if not shiftlockWasEnabled then
            if shiftlockLoaded and SmoothShiftLock and not SmoothShiftLock.Enabled then
                pcall(function()
                    SmoothShiftLock:ToggleShiftLock()
                end)
            else
                pcall(function()
                    local shiftlockKeybindEnum = lp.PlayerData.Settings.Keybinds.ShiftLock.Value
                    local shiftlockKeyCode = Enum.KeyCode[shiftlockKeybindEnum]
                    
                    if shiftlockKeyCode then
                        VirtualInputManager:SendKeyEvent(true, shiftlockKeyCode, false, game)
                        VirtualInputManager:SendKeyEvent(false, shiftlockKeyCode, false, game)
                    end
                end)
            end
        end
    else
        if isShiftLockEnabled() then
            if shiftlockLoaded and SmoothShiftLock and SmoothShiftLock.Enabled then
                pcall(function()
                    SmoothShiftLock:ToggleShiftLock()
                end)
            else
                pcall(function()
                    local shiftlockKeybindEnum = lp.PlayerData.Settings.Keybinds.ShiftLock.Value
                    local shiftlockKeyCode = Enum.KeyCode[shiftlockKeybindEnum]
                    
                    if shiftlockKeyCode then
                        VirtualInputManager:SendKeyEvent(true, shiftlockKeyCode, false, game)
                        VirtualInputManager:SendKeyEvent(false, shiftlockKeyCode, false, game)
                    end
                end)
            end
        end
    end
end

-- ===== PERFORMANCE CACHES =====
local cachedBlockBtn, cachedChargeBtn, cachedCloneBtn
local cachedCooldown
local detectionCircles = {}
local survivorCircles = {}
local _lastBlockMessageTime = 0
local MESSAGE_BLOCK_COOLDOWN = 0.6

-- Timers for auto block
local lastBlockTime = 0
local lastCloneTime = 0
local lastSpecialAbilityTime = 0
local BLOCK_COOLDOWN = 0.7
local CLONE_COOLDOWN = 0.7
local SPECIAL_ABILITY_COOLDOWN = 1.0

-- ===== UTILITY FUNCTIONS =====
local function SendNotif(title, text, duration)
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = title or "Auto Block",
            Text = text or "Notification",
            Duration = duration or 4
        })
    end)
end

local function pressKey(keyCode)
    VirtualInputManager:SendKeyEvent(true, keyCode, false, game)
    task.wait(0.05)
    VirtualInputManager:SendKeyEvent(false, keyCode, false, game)
end

-- ===== AUTO BLOCK UI REFRESH =====
local function refreshUIRefs()
    local main = PlayerGui:FindFirstChild("MainUI")
    if main then
        local ability = main:FindFirstChild("AbilityContainer")
        if ability then
            cachedBlockBtn = ability:FindFirstChild("Block")
            cachedChargeBtn = ability:FindFirstChild("Charge")
            cachedCloneBtn = ability:FindFirstChild("Clone")
            
            if cachedBlockBtn then
                cachedCooldown = cachedBlockBtn:FindFirstChild("CooldownTime")
            end
        end
    end
end

local function isHelpless()
    local success, helplessObj = pcall(function()
        return lp.PlayerGui.MainUI.AbilityContainer.Block:FindFirstChild("Helpless")
    end)
    
    if success and helplessObj then
        return true
    end
    return false
end

-- ===== CHANCE-BASED BLOCKING =====
local function shouldBlock()
    if blockChance >= 100 then return true end
    if blockChance <= 0 then return false end
    return math.random(1, 100) <= blockChance
end

-- ===== FACING CHECK =====
local function isFacing(localRoot, targetRoot)
    if not facingCheckEnabled then return true end
    
    local dx = localRoot.Position.X - targetRoot.Position.X
    local dy = localRoot.Position.Y - targetRoot.Position.Y
    local dz = localRoot.Position.Z - targetRoot.Position.Z
    
    local mag = math.sqrt(dx*dx + dy*dy + dz*dz)
    if mag == 0 then return true end
    local invMag = 1 / mag
    
    local ux, uy, uz = dx * invMag, dy * invMag, dz * invMag
    local lv = targetRoot.CFrame.LookVector
    local dot = lv.X * ux + lv.Y * uy + lv.Z * uz
    
    return dot > customFacingDot
end

-- ===== AUTO-SWITCH BLOCK/CHARGE =====
local lastBlockCheckTime = 0
local BLOCK_CHECK_INTERVAL = 0.2

local function checkAndSwitchBlockType()
    if not autoSwitchBlockCharge then return end
    
    local currentTime = tick()
    if currentTime - lastBlockCheckTime < BLOCK_CHECK_INTERVAL then return end
    lastBlockCheckTime = currentTime
    
    refreshUIRefs()
    
    if not cachedBlockBtn then return end
    
    local blockCooldown = cachedBlockBtn:FindFirstChild("CooldownTime")
    if not blockCooldown then return end
    
    if blockCooldown.Text ~= "" and blockCooldown.Text ~= "0" then
        if autoblocktype ~= "Charge" then
            originalBlockType = autoblocktype
            autoblocktype = "Charge"
        end
    else
        if autoblocktype == "Charge" and originalBlockType ~= "Charge" then
            autoblocktype = originalBlockType
        end
    end
end

-- ===== EXTRACT IDs =====
local function extractNumericSoundId(sound)
    if not sound then return nil end
    local sid = tostring(sound.SoundId)
    
    local num = sid:match("rbxassetid://(%d+)") or
                sid:match("://(%d+)") or
                sid:match("^(%d+)$")
    return num
end

local function extractNumericAnimationId(animationId)
    if not animationId then return nil end
    local id = tostring(animationId)
    local num = id:match("rbxassetid://(%d+)") or
                id:match("://(%d+)") or
                id:match("^(%d+)$")
    return num
end

-- ===== CHARACTER IDENTIFICATION FUNCTIONS =====
local function getMyCharacterInfo()
    if SurvivorsFolder then
        for _, survivor in ipairs(SurvivorsFolder:GetChildren()) do
            local usernameAttr = survivor:GetAttribute("Username")
            if usernameAttr and usernameAttr == lp.Name then
                return "Survivor", survivor.Name, survivor
            end
        end
    end
    
    if KillersFolder then
        for _, killer in ipairs(KillersFolder:GetChildren()) do
            local usernameAttr = killer:GetAttribute("Username")
            if usernameAttr and usernameAttr == lp.Name then
                return "Killer", killer.Name, killer
            end
        end
    end
    
    return nil, nil, nil
end

local function isLocalPlayer(survivor)
    local usernameAttr = survivor:GetAttribute("Username")
    return usernameAttr and usernameAttr == lp.Name
end

local function amIGuest1337()
    if SurvivorsFolder then
        for _, survivor in ipairs(SurvivorsFolder:GetChildren()) do
            if survivor.Name == "Guest1337" then
                local usernameAttr = survivor:GetAttribute("Username")
                if usernameAttr and usernameAttr == lp.Name then
                    return true, survivor
                end
            end
        end
    end
    return false, nil
end

local function amI7n7()
    if SurvivorsFolder then
        for _, survivor in ipairs(SurvivorsFolder:GetChildren()) do
            if survivor.Name == "007n7" or survivor.Name == "7n7" then
                local usernameAttr = survivor:GetAttribute("Username")
                if usernameAttr and usernameAttr == lp.Name then
                    return true, survivor
                end
            end
        end
    end
    return false, nil
end

local function amISlasher()
    if KillersFolder then
        for _, killer in ipairs(KillersFolder:GetChildren()) do
            if killer.Name == "Slasher" then
                local usernameAttr = killer:GetAttribute("Username")
                if usernameAttr and usernameAttr == lp.Name then
                    return true, killer
                end
            end
        end
    end
    return false, nil
end

local function amIJohnDoe()
    if KillersFolder then
        for _, killer in ipairs(KillersFolder:GetChildren()) do
            if killer.Name == "JohnDoe" then
                local usernameAttr = killer:GetAttribute("Username")
                if usernameAttr and usernameAttr == lp.Name then
                    return true, killer
                end
            end
        end
    end
    return false, nil
end

local function amI1x1x1x1()
    if KillersFolder then
        for _, killer in ipairs(KillersFolder:GetChildren()) do
            if killer.Name == "1x1x1x1" then
                local usernameAttr = killer:GetAttribute("Username")
                if usernameAttr and usernameAttr == lp.Name then
                    return true, killer
                end
            end
        end
    end
    return false, nil
end

local function amITwoTime()
    if SurvivorsFolder then
        for _, survivor in ipairs(SurvivorsFolder:GetChildren()) do
            if survivor.Name == "TwoTime" then
                local usernameAttr = survivor:GetAttribute("Username")
                if usernameAttr and usernameAttr == lp.Name then
                    return true, survivor
                end
            end
        end
    end
    return false, nil
end

local function amISixer()
    if KillersFolder then
        for _, killer in ipairs(KillersFolder:GetChildren()) do
            if killer.Name == "Sixer" then
                local usernameAttr = killer:GetAttribute("Username")
                if usernameAttr and usernameAttr == lp.Name then
                    return true, killer
                end
            end
        end
    end
    return false, nil
end

local function amIElliot()
    if SurvivorsFolder then
        for _, survivor in ipairs(SurvivorsFolder:GetChildren()) do
            if survivor.Name == "Elliot" then
                local usernameAttr = survivor:GetAttribute("Username")
                if usernameAttr and usernameAttr == lp.Name then
                    return true, survivor
                end
            end
        end
    end
    return false, nil
end

local function amIDusekkar()
    if SurvivorsFolder then
        for _, survivor in ipairs(SurvivorsFolder:GetChildren()) do
            if survivor.Name == "Dusekkar" then
                local usernameAttr = survivor:GetAttribute("Username")
                if usernameAttr and usernameAttr == lp.Name then
                    return true
                end
            end
        end
    end
    return false
end

local function getAllCharactersByName(name)
    local characters = {}
    
    if name == "Guest1337" or name == "Shedletsky" or name == "TwoTime" or name == "Chance" or name == "JaneDoe" or name == "Elliot" then
        if SurvivorsFolder then
            for _, survivor in ipairs(SurvivorsFolder:GetChildren()) do
                if survivor.Name == name then
                    table.insert(characters, survivor)
                end
            end
        end
    elseif name == "Slasher" or name == "JohnDoe" or name == "1x1x1x1" or name == "Sixer" then
        if KillersFolder then
            for _, killer in ipairs(KillersFolder:GetChildren()) do
                if killer.Name == name then
                    table.insert(characters, killer)
                end
            end
        end
    end
    
    return characters
end

-- ===== ROTATION FUNCTION =====
local function rotateTowardsKiller(killerModel)
    if rotationActive then return end
    if not killerModel then return end
    
    local char = lp.Character
    if not char then return end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    local killerHrp = killerModel:FindFirstChild("HumanoidRootPart")
    if not killerHrp then return end
    
    rotationActive = true
    
    local myPos = hrp.Position
    local killerPos = killerHrp.Position
    local direction = (Vector3.new(killerPos.X, myPos.Y, killerPos.Z) - myPos).Unit
    
    hrp.CFrame = CFrame.lookAt(myPos, myPos + direction)
    
    task.delay(ROTATION_DURATION, function()
        rotationActive = false
    end)
end

-- ===== PREDICTION FUNCTIONS =====
local function updatePositionHistory(character, position)
    if not character or not position then return end
    
    if not positionHistory[character] then
        positionHistory[character] = {}
    end
    
    local history = positionHistory[character]
    table.insert(history, position)
    
    if #history > HISTORY_SIZE then
        table.remove(history, 1)
    end
end

local function getMovementDirectionFromHistory(character)
    if not character or not positionHistory[character] then
        return Vector3.new(0,0,0)
    end
    
    local history = positionHistory[character]
    if #history < 2 then
        return Vector3.new(0,0,0)
    end
    
    local oldestPos = history[1]
    local newestPos = history[#history]
    
    local movement = newestPos - oldestPos
    local magnitude = movement.Magnitude
    
    local recentMovement = Vector3.new(0,0,0)
    if #history >= 3 then
        recentMovement = history[#history] - history[#history-1]
    end
    
    if magnitude > MOVE_THRESHOLD then
        return movement.Unit
    elseif recentMovement.Magnitude > MOVE_THRESHOLD then
        return recentMovement.Unit
    end
    
    return Vector3.new(0,0,0)
end

local function getInstantaneousDirection(character, currentPos)
    if not character or not currentPos then return Vector3.new(0,0,0) end
    
    if not positionHistory[character] or #positionHistory[character] < 2 then
        return Vector3.new(0,0,0)
    end
    
    local history = positionHistory[character]
    local lastPos = history[#history]
    
    local movement = currentPos - lastPos
    if movement.Magnitude > MOVE_THRESHOLD then
        return movement.Unit
    end
    
    return Vector3.new(0,0,0)
end

local function getPredictedPosition(character)
    if not character then return nil end
    
    local root = character:FindFirstChild("HumanoidRootPart") or 
                 character:FindFirstChild("Head") or 
                 character:FindFirstChild("Torso")
    if not root then return nil end
    
    local currentPos = root.Position
    
    updatePositionHistory(character, currentPos)
    
    local moveDir = getMovementDirectionFromHistory(character)
    local instantDir = getInstantaneousDirection(character, currentPos)
    
    local finalDir = instantDir
    if instantDir.Magnitude <= MOVE_THRESHOLD then
        finalDir = moveDir
    end
    
    local currentPredictionDistance = BASE_PREDICTION_DISTANCE * aimbotPredictionMultiplier
    
    if finalDir.Magnitude > MOVE_THRESHOLD then
        return currentPos + (finalDir * currentPredictionDistance)
    else
        return currentPos
    end
end

local function createTargetSphere(targetCharacter, sphereColor)
    if currentTargetSphere then
        pcall(function() currentTargetSphere:Destroy() end)
        currentTargetSphere = nil
    end
    if currentSphereConnection then
        pcall(function() currentSphereConnection:Disconnect() end)
        currentSphereConnection = nil
    end
    
    if not targetCharacter then return nil end
    
    local root = targetCharacter:FindFirstChild("HumanoidRootPart") or 
                 targetCharacter:FindFirstChild("Head") or 
                 targetCharacter:FindFirstChild("Torso")
    if not root then return nil end

    local sphere = Instance.new("Part")
    sphere.Shape = Enum.PartType.Ball
    sphere.Size = Vector3.new(2, 2, 2)
    sphere.Material = Enum.Material.Neon
    sphere.Color = sphereColor or Color3.fromRGB(255, 0, 0)
    sphere.Anchored = true
    sphere.CanCollide = false
    sphere.Name = "TargetSphere"
    sphere.Parent = workspace
    sphere.Position = root.Position

    updatePositionHistory(targetCharacter, root.Position)

    local connection
    connection = RunService.Heartbeat:Connect(function()
        if not targetCharacter or not targetCharacter.Parent or not sphere.Parent then
            sphere:Destroy()
            if connection then
                connection:Disconnect()
            end
            currentTargetSphere = nil
            currentSphereConnection = nil
            return
        end

        local humanoid = targetCharacter:FindFirstChildOfClass("Humanoid")
        if not humanoid or humanoid.Health <= 0 then
            sphere:Destroy()
            if connection then
                connection:Disconnect()
            end
            currentTargetSphere = nil
            currentSphereConnection = nil
            return
        end

        root = targetCharacter:FindFirstChild("HumanoidRootPart") or 
               targetCharacter:FindFirstChild("Head") or 
               targetCharacter:FindFirstChild("Torso")
        if not root then
            sphere:Destroy()
            if connection then
                connection:Disconnect()
            end
            currentTargetSphere = nil
            currentSphereConnection = nil
            return
        end

        local currentPos = root.Position
        
        updatePositionHistory(targetCharacter, currentPos)
        
        local moveDir = getMovementDirectionFromHistory(targetCharacter)
        local instantDir = getInstantaneousDirection(targetCharacter, currentPos)
        
        local finalDir = instantDir
        if instantDir.Magnitude <= MOVE_THRESHOLD then
            finalDir = moveDir
        end
        
        local currentPredictionDistance = BASE_PREDICTION_DISTANCE * aimbotPredictionMultiplier

        local targetPosition

        if finalDir.Magnitude > MOVE_THRESHOLD then
            targetPosition = currentPos + (finalDir * currentPredictionDistance)
        else
            targetPosition = currentPos
        end

        sphere.Position = sphere.Position:Lerp(targetPosition, SPHERE_SMOOTHNESS)
    end)

    currentTargetSphere = sphere
    currentSphereConnection = connection
    return sphere, connection
end

-- ===== GET VALID TARGETS FOR AIMBOT =====
local function getValidTargets()
    local targets = {}
    local myChar = lp.Character
    if not myChar then return targets end
    
    local myRoot = myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return targets end
    
    local myTeam, myCharacter = getMyCharacterInfo()
    local isElliot = (myTeam == "Survivor" and myCharacter == "Elliot")
    
    local viewportSize = Camera.ViewportSize
    local screenCenter = Vector2.new(viewportSize.X/2, viewportSize.Y/2)
    
    local function processTarget(obj)
        local usernameAttr = obj:GetAttribute("Username")
        if usernameAttr and usernameAttr == lp.Name then
            return
        end
        
        local humanoid = obj:FindFirstChildOfClass("Humanoid")
        if not humanoid or humanoid.Health <= 0 then
            return
        end
        
        local targetPart = obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Head") or obj:FindFirstChild("Torso")
        if not targetPart then return end
        
        local distance = (targetPart.Position - myRoot.Position).Magnitude
        if distance > aimbotMaxDistance then
            return
        end
        
        if aimbotUseFOV then
            local screenPos, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
            if onScreen then
                local screenVector = Vector2.new(screenPos.X, screenPos.Y)
                local distanceFromCenter = (screenVector - screenCenter).Magnitude
                
                if distanceFromCenter <= aimbotFOV then
                    table.insert(targets, {
                        obj = obj,
                        part = targetPart,
                        distance = distance,
                        screenDistance = distanceFromCenter
                    })
                end
            end
        else
            table.insert(targets, {
                obj = obj,
                part = targetPart,
                distance = distance,
                screenDistance = 0
            })
        end
    end
    
    if isElliot then
        for _, survivor in ipairs(SurvivorsFolder:GetChildren()) do
            processTarget(survivor)
        end
    elseif myTeam == "Killer" then
        for _, survivor in ipairs(SurvivorsFolder:GetChildren()) do
            processTarget(survivor)
        end
    elseif myTeam == "Survivor" then
        for _, killer in ipairs(KillersFolder:GetChildren()) do
            processTarget(killer)
        end
    end
    
    if aimbotUseFOV then
        table.sort(targets, function(a, b)
            return a.screenDistance < b.screenDistance
        end)
    else
        table.sort(targets, function(a, b)
            return a.distance < b.distance
        end)
    end
    
    return targets
end

-- ===== GET NEAREST KILLER FOR TWOTIME =====
local function getNearestKiller()
    local myChar = lp.Character
    if not myChar then return nil end
    
    local myRoot = myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return nil end
    
    local nearestKiller = nil
    local nearestDistance = math.huge
    
    for _, killer in ipairs(KillersFolder:GetChildren()) do
        local usernameAttr = killer:GetAttribute("Username")
        if not (usernameAttr and usernameAttr == lp.Name) then
            local targetHrp = killer:FindFirstChild("HumanoidRootPart")
            if targetHrp then
                local humanoid = killer:FindFirstChildOfClass("Humanoid")
                if humanoid and humanoid.Health > 0 then
                    local distance = (targetHrp.Position - myRoot.Position).Magnitude
                    if distance < nearestDistance then
                        nearestDistance = distance
                        nearestKiller = killer
                    end
                end
            end
        end
    end
    
    return nearestKiller
end

-- ===== TWOTIME FUNCTIONS =====
local function isTwoTimeDaggerReady()
    local mainUI = PlayerGui:FindFirstChild("MainUI")
    if mainUI then
        local abilityContainer = mainUI:FindFirstChild("AbilityContainer")
        if abilityContainer then
            local dagger = abilityContainer:FindFirstChild("Dagger")
            if dagger then
                local cooldownTime = dagger:FindFirstChild("CooldownTime")
                if cooldownTime then
                    local contentText = cooldownTime.ContentText
                    return contentText == "" or contentText == "0" or contentText == "0.0" or contentText == "0.1"
                end
            end
        end
    end
    return true
end

local function isCrouching()
    local mainUI = PlayerGui:FindFirstChild("MainUI")
    if mainUI then
        local statusContainer = mainUI:FindFirstChild("StatusContainer")
        if statusContainer then
            local invisibility = statusContainer:FindFirstChild("Invisibility")
            local undetectable = statusContainer:FindFirstChild("Undetectable")
            
            if invisibility and invisibility.Visible == true then
                return true
            end
            
            if undetectable and undetectable.Visible == true then
                return true
            end
        end
    end
    return false
end

local function stopAimbot()
    aimbotActive = false
    aimbotCurrentTarget = nil
    
    if aimbotActiveTimer then
        pcall(function() task.cancel(aimbotActiveTimer) end)
        aimbotActiveTimer = nil
    end
    
    if currentTargetSphere then
        pcall(function() currentTargetSphere:Destroy() end)
        currentTargetSphere = nil
    end
    if currentSphereConnection then
        pcall(function() currentSphereConnection:Disconnect() end)
        currentSphereConnection = nil
    end
    setShiftLock(false)
end

-- ===== NORMAL STAB =====
local function startNormalStab(targetKiller)
    if not targetKiller then return end
    
    local char = lp.Character
    if not char then return end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    if faceSameSideAsKiller then
        setShiftLock(false)
        createTargetSphere(targetKiller, Color3.fromRGB(255, 0, 0))
        
        local backstabConnection
        backstabConnection = RunService.Heartbeat:Connect(function()
            if not aimbotActive then
                if backstabConnection then backstabConnection:Disconnect() end
                return
            end
            
            if not targetKiller or not targetKiller.Parent then
                stopAimbot()
                if backstabConnection then backstabConnection:Disconnect() end
                return
            end
            
            local killerHrp = targetKiller:FindFirstChild("HumanoidRootPart")
            if killerHrp then
                local killerLook = killerHrp.CFrame.LookVector
                if killerLook.Magnitude > 0 then
                    hrp.CFrame = CFrame.lookAt(hrp.Position, hrp.Position + killerLook)
                end
            end
        end)
        
        if aimbotActiveTimer then
            pcall(function() task.cancel(aimbotActiveTimer) end)
        end
        aimbotActiveTimer = task.delay(0.4, function()
            aimbotActive = false
            aimbotCurrentTarget = nil
            if backstabConnection then backstabConnection:Disconnect() end
            if currentTargetSphere then
                pcall(function() currentTargetSphere:Destroy() end)
                currentTargetSphere = nil
            end
        end)
    else
        setShiftLock(true)
        createTargetSphere(targetKiller, Color3.fromRGB(255, 0, 0))
        
        local cameraConnection
        cameraConnection = RunService.RenderStepped:Connect(function()
            if not aimbotActive then
                if cameraConnection then cameraConnection:Disconnect() end
                return
            end
            
            if not targetKiller or not targetKiller.Parent then
                stopAimbot()
                if cameraConnection then cameraConnection:Disconnect() end
                return
            end
            
            local targetPos = nil
            if currentTargetSphere and currentTargetSphere.Parent then
                targetPos = currentTargetSphere.Position
            else
                local targetHrp = targetKiller:FindFirstChild("HumanoidRootPart")
                if targetHrp then
                    targetPos = targetHrp.Position
                else
                    stopAimbot()
                    if cameraConnection then cameraConnection:Disconnect() end
                    return
                end
            end
            
            local camera = Camera
            local cameraPos = camera.CFrame.Position
            local targetCFrame = CFrame.lookAt(cameraPos, targetPos)
            local currentCFrame = camera.CFrame            
            local currentLook = currentCFrame.LookVector
            local targetLook = targetCFrame.LookVector
            local smoothLook = currentLook:Lerp(targetLook, aimbotSmoothness)
            local newCFrame = CFrame.new(cameraPos, cameraPos + smoothLook)
            
            camera.CFrame = newCFrame
        end)
        
        if aimbotActiveTimer then
            pcall(function() task.cancel(aimbotActiveTimer) end)
        end
        aimbotActiveTimer = task.delay(0.4, function()
            aimbotActive = false
            aimbotCurrentTarget = nil
            if cameraConnection then cameraConnection:Disconnect() end
            if currentTargetSphere then
                pcall(function() currentTargetSphere:Destroy() end)
                currentTargetSphere = nil
            end
            setShiftLock(false)
        end)
    end
end

-- ===== DASH STAB =====
local function startDashStab(targetKiller)
    if not targetKiller then return end
    
    local char = lp.Character
    if not char then return end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    aimbotActive = true
    aimbotCurrentTarget = targetKiller
    
    setShiftLock(true)
    createTargetSphere(targetKiller, Color3.fromRGB(0, 255, 0))
    
    local dashConnection
    dashConnection = RunService.RenderStepped:Connect(function()
        if not aimbotActive then
            if dashConnection then dashConnection:Disconnect() end
            return
        end
        
        if not targetKiller or not targetKiller.Parent then
            stopAimbot()
            if dashConnection then dashConnection:Disconnect() end
            return
        end
        
        local targetPos = nil
        if currentTargetSphere and currentTargetSphere.Parent then
            targetPos = currentTargetSphere.Position
        else
            local targetHrp = targetKiller:FindFirstChild("HumanoidRootPart")
            if targetHrp then
                targetPos = targetHrp.Position
            else
                stopAimbot()
                if dashConnection then dashConnection:Disconnect() end
                return
            end
        end
        
        local camera = Camera
        local cameraPos = camera.CFrame.Position
        local targetCFrame = CFrame.lookAt(cameraPos, targetPos)
        local currentCFrame = camera.CFrame
        
        local currentLook = currentCFrame.LookVector
        local targetLook = targetCFrame.LookVector
        local smoothLook = currentLook:Lerp(targetLook, aimbotSmoothness)
        local newCFrame = CFrame.new(cameraPos, cameraPos + smoothLook)
        
        camera.CFrame = newCFrame
    end)
    
    task.delay(0.35, function()
        if not aimbotActive then return end
        
        setShiftLock(false)
        
        if dashConnection then
            dashConnection:Disconnect()
        end
        
        if currentTargetSphere then
            currentTargetSphere.Color = Color3.fromRGB(255, 0, 0)
        end
        
        local backstabConnection
        backstabConnection = RunService.Heartbeat:Connect(function()
            if not aimbotActive then
                if backstabConnection then backstabConnection:Disconnect() end
                return
            end
            
            if not targetKiller or not targetKiller.Parent then
                stopAimbot()
                if backstabConnection then backstabConnection:Disconnect() end
                return
            end
            
            local killerHrp = targetKiller:FindFirstChild("HumanoidRootPart")
            if killerHrp then
                local killerLook = killerHrp.CFrame.LookVector
                if killerLook.Magnitude > 0 then
                    hrp.CFrame = CFrame.lookAt(hrp.Position, hrp.Position + killerLook)
                end
            end
        end)
        
        task.delay(0.4, function()
            aimbotActive = false
            aimbotCurrentTarget = nil
            if backstabConnection then backstabConnection:Disconnect() end
            if currentTargetSphere then
                pcall(function() currentTargetSphere:Destroy() end)
                currentTargetSphere = nil
            end
        end)
    end)
end

-- ===== SIXER CARVING SLASH CHARGES =====
local function getSixerCarvingSlashCharges()
    local success, charges = pcall(function()
        local abilityContainer = lp.PlayerGui.MainUI.AbilityContainer
        if abilityContainer then
            local carvingSlash = abilityContainer:FindFirstChild("Carving Slash") or abilityContainer:FindFirstChild("CarvingSlash")
            if carvingSlash then
                local chargesObj = carvingSlash:FindFirstChild("Charges")
                if chargesObj then
                    local contextText = chargesObj:FindFirstChild("ContextText")
                    if contextText then
                        return tonumber(contextText.Text) or 0
                    end
                end
            end
        end
        return 0
    end)
    if success then
        return charges
    end
    return 0
end

-- ===== GENERAL AIMBOT FUNCTIONS =====
local function startGeneralAimbot(duration, customSmoothness)
    if not aimbotEnabled then return end
    if aimbotActive then return end
    
    local targets = getValidTargets()
    if #targets == 0 then return end
    
    local target = targets[1]
    aimbotCurrentTarget = target.obj
    aimbotActive = true
    aimbotTargetCount = aimbotTargetCount + 1
    
    setShiftLock(true)
    
    local originalSmoothness = aimbotSmoothness
    if customSmoothness then
        aimbotSmoothness = customSmoothness
    end
    
    createTargetSphere(aimbotCurrentTarget, Color3.fromRGB(255, 0, 0))
    
    local cameraConnection
    cameraConnection = RunService.RenderStepped:Connect(function()
        if not aimbotActive or not aimbotCurrentTarget then
            if cameraConnection then cameraConnection:Disconnect() end
            return
        end
        
        local myChar = lp.Character
        if not myChar then return end
        
        local humanoid = aimbotCurrentTarget:FindFirstChildOfClass("Humanoid")
        if not humanoid or humanoid.Health <= 0 then
            stopAimbot()
            if cameraConnection then cameraConnection:Disconnect() end
            return
        end
        
        local targetPos = getPredictedPosition(aimbotCurrentTarget)
        if not targetPos then
            stopAimbot()
            if cameraConnection then cameraConnection:Disconnect() end
            return
        end
        
        local camera = Camera
        local cameraPos = camera.CFrame.Position
        local targetCFrame = CFrame.lookAt(cameraPos, targetPos)
        local currentCFrame = camera.CFrame
        
        local currentLook = currentCFrame.LookVector
        local targetLook = targetCFrame.LookVector
        local smoothLook = currentLook:Lerp(targetLook, aimbotSmoothness)
        local newCFrame = CFrame.new(cameraPos, cameraPos + smoothLook)
        
        camera.CFrame = newCFrame
    end)
    
    if aimbotActiveTimer then
        pcall(function() task.cancel(aimbotActiveTimer) end)
    end
    
    aimbotActiveTimer = task.delay(duration, function()
        if customSmoothness then
            aimbotSmoothness = originalSmoothness
        end
        aimbotActive = false
        aimbotCurrentTarget = nil
        if cameraConnection then cameraConnection:Disconnect() end
        if currentTargetSphere then
            pcall(function() currentTargetSphere:Destroy() end)
            currentTargetSphere = nil
        end
        setShiftLock(false)
    end)
end

local function updateAimbot()
    if not aimbotEnabled then
        if aimbotConnection then
            aimbotConnection:Disconnect()
            aimbotConnection = nil
        end
        stopAimbot()
        return
    end
end

local function createFOVCircle()
    if aimbotFOVCircle then
        pcall(function() aimbotFOVCircle:Destroy() end)
        aimbotFOVCircle = nil
    end
    
    if not aimbotEnabled or not aimbotUseFOV then return end
    
    local circle = Drawing.new("Circle")
    circle.Transparency = 1
    circle.Thickness = 2
    circle.NumSides = 60
    circle.Radius = aimbotFOV
    circle.Color = Color3.fromRGB(255, 255, 255)
    circle.Visible = true
    circle.Filled = false
    circle.Visible = aimbotEnabled and aimbotUseFOV
    
    aimbotFOVCircle = circle
    
    local connection
    connection = RunService.RenderStepped:Connect(function()
        if aimbotEnabled and aimbotUseFOV and aimbotFOVCircle then
            local viewportSize = Camera.ViewportSize
            aimbotFOVCircle.Position = Vector2.new(viewportSize.X/2, viewportSize.Y/2)
            aimbotFOVCircle.Radius = aimbotFOV
            aimbotFOVCircle.Visible = true
        elseif aimbotFOVCircle then
            aimbotFOVCircle.Visible = false
        end
    end)
end

-- ===== AUTO BLOOD ORB FUNCTIONS =====
local function bringBloodOrbsToPlayer()
    if not autoBloodOrb then return end
    
    local isSixer = amISixer()
    if not isSixer then return end
    
    local bloodUI = PlayerGui:FindFirstChild("TemporaryUI")
    if bloodUI then
        bloodUI = bloodUI:FindFirstChild("BloodUI")
    end
    
    if bloodUI then
        local blood = bloodUI:FindFirstChild("Blood")
        local timer = bloodUI:FindFirstChild("Timer")
        
        if blood then
            local bloodCount = blood:FindFirstChild("Count")
            local bloodMax = blood:FindFirstChild("Max")
            
            if bloodCount and bloodMax then
                local currentCount = bloodCount:FindFirstChild("ContentText")
                local maxCount = bloodMax:FindFirstChild("ContextText")
                
                if currentCount and maxCount then
                    if currentCount.Text == maxCount.Text then
                        return
                    end
                end
            end
        end
        
        if timer then
            local timeText = timer:FindFirstChild("Time")
            if timeText then
                local contentText = timeText:FindFirstChild("ContentText")
                if contentText then
                    local timeValue = contentText.Text
                    if timeValue ~= "0" and timeValue ~= "TIME'S UP" then
                        return
                    end
                end
            end
        end
    end
    
    local bloodOrbContainer = Workspace:FindFirstChild("Map")
    if bloodOrbContainer then
        bloodOrbContainer = bloodOrbContainer:FindFirstChild("vrrebluxBloodOrbContainer")
    end
    
    if not bloodOrbContainer then return end
    
    local char = lp.Character
    if not char then return end
    
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    for _, orb in ipairs(bloodOrbContainer:GetChildren()) do
        if orb.Name == "BloodOrb" and orb:IsA("BasePart") then
            pcall(function()
                local tweenInfo = TweenInfo.new(0.3, Enum.EasingStyle.Linear)
                local tween = TweenService:Create(orb, tweenInfo, {Position = hrp.Position})
                tween:Play()
                tween.Completed:Wait()
            end)
        end
    end
end

local function startBloodOrbLoop()
    if bloodOrbConnection then
        bloodOrbConnection:Disconnect()
        bloodOrbConnection = nil
    end
    
    if not autoBloodOrb then return end
    
    bloodOrbConnection = RunService.Heartbeat:Connect(function()
        pcall(bringBloodOrbsToPlayer)
    end)
end

-- ===== GUEST1337 AUTO BLOCK =====
local function checkForAttackSounds()
    if not autoBlockForGuest then return end
    
    local now = tick()
    if now - lastBlockTime < BLOCK_COOLDOWN then return end
    
    local myChar = lp.Character
    local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return end
    
    if not KillersFolder then return end
    
    local isGuest1337, guestChar = amIGuest1337()
    if not isGuest1337 then return end
    
    for _, killer in ipairs(KillersFolder:GetChildren()) do
        local usernameAttr = killer:GetAttribute("Username")
        if not (usernameAttr and usernameAttr == lp.Name) then
            
            local killerHrp = killer:FindFirstChild("HumanoidRootPart")
            if killerHrp then
                local distance = (killerHrp.Position - myRoot.Position).Magnitude
                
                if distance <= detectionRange then
                    if (not facingCheckEnabled) or isFacing(myRoot, killerHrp) then
                        if shouldBlock() then
                            
                            local shouldBlockThisAttack = false
                            
                            if killer.Name == "Slasher" then
                                local humanoid = killer:FindFirstChildOfClass("Humanoid")
                                if humanoid then
                                    local animator = humanoid:FindFirstChildOfClass("Animator")
                                    if animator then
                                        for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                                            local animId = extractNumericAnimationId(track.Animation.AnimationId)
                                            if animId and autoBlockTriggerAnimations[animId] then
                                                shouldBlockThisAttack = true
                                                break
                                            end
                                        end
                                    end
                                end
                            end
                            
                            if not shouldBlockThisAttack then
                                for _, sound in ipairs(killer:GetDescendants()) do
                                    if sound:IsA("Sound") and sound.IsPlaying then
                                        local soundId = extractNumericSoundId(sound)
                                        if soundId and autoBlockTriggerSounds[soundId] then
                                            shouldBlockThisAttack = true
                                            break
                                        end
                                    end
                                end
                            end
                            
                            if shouldBlockThisAttack then
                                if blockdelay > 0 then
                                    task.wait(blockdelay)
                                end
                                
                                refreshUIRefs()
                                
                                if isHelpless() then
                                    break
                                end
                                
                                if autoblocktype == "Block" then
                                    if cachedCooldown and cachedCooldown.Text == "" then
                                        pressKey(Enum.KeyCode.Q)
                                        lastBlockTime = now
                                        return
                                    end
                                elseif autoblocktype == "Charge" then
                                    if cachedChargeBtn and cachedChargeBtn:FindFirstChild("CooldownTime") and cachedChargeBtn.CooldownTime.Text == "" then
                                        pressKey(Enum.KeyCode.Q)
                                        lastBlockTime = now
                                        return
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end
    end
end

-- ===== 7N7 AUTO CLONE =====
local function checkForCloneTrigger()
    if not autoCloneFor7n7 then return end
    
    local now = tick()
    if now - lastCloneTime < CLONE_COOLDOWN then return end
    
    local myChar = lp.Character
    local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return end
    
    if not KillersFolder then return end
    
    local is7n7, sevenNChar = amI7n7()
    if not is7n7 then return end
    
    for _, killer in ipairs(KillersFolder:GetChildren()) do
        local usernameAttr = killer:GetAttribute("Username")
        if not (usernameAttr and usernameAttr == lp.Name) then
            local killerHrp = killer:FindFirstChild("HumanoidRootPart")
            if killerHrp then
                local distance = (killerHrp.Position - myRoot.Position).Magnitude
                
                if distance <= detectionRange then
                    if (not facingCheckEnabled) or isFacing(myRoot, killerHrp) then
                        for _, sound in ipairs(killer:GetDescendants()) do
                            if sound:IsA("Sound") and sound.IsPlaying then
                                local soundId = extractNumericSoundId(sound)
                                if soundId and autoBlockTriggerSounds[soundId] then
                                    
                                    if blockdelay > 0 then
                                        task.wait(blockdelay)
                                    end
                                    
                                    refreshUIRefs()
                                    
                                    if cachedCloneBtn and cachedCloneBtn:FindFirstChild("CooldownTime") and cachedCloneBtn.CooldownTime.Text == "" then
                                        pressKey(Enum.KeyCode.Q)
                                        rotateTowardsKiller(killer)
                                        lastCloneTime = now
                                        return
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end
    end
end

-- ===== SIXER BLOOD HUNT AUTO PARRY =====
local function checkSixerBloodHuntParry()
    local isSixer = amISixer()
    if not isSixer then return end
    if not bloodHuntAutoParry then return end
    
    local now = tick()
    if now - lastSpecialAbilityTime < SPECIAL_ABILITY_COOLDOWN then return end
    
    local myChar = lp.Character
    if not myChar then return end
    
    local myRoot = myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return end
    
    local mainUI = PlayerGui:FindFirstChild("MainUI")
    if mainUI then
        local abilityContainer = mainUI:FindFirstChild("AbilityContainer")
        if abilityContainer then
            local bloodHunt = abilityContainer:FindFirstChild("BloodHunt")
            if bloodHunt then
                local hunting = bloodHunt:FindFirstChild("Hunting")
                if hunting then
                    return
                end
                
                local abilityName = bloodHunt:FindFirstChild("AbilityName")
                if abilityName then
                    local contentText = abilityName.ContentText
                    if contentText ~= "Blood Hunt" then
                        return
                    end
                else
                    return
                end
            else
                return
            end
        else
            return
        end
    else
        return
    end
    
    local triggerSoundIds = {
        ["12222225"] = true,
        ["132596270805754"] = true,
        ["83851356262523"] = true,
        ["111910850942168"] = true,
        ["115267992424648"] = true,
        ["99820161736138"] = true,
        ["86710781315432"] = true,
        ["139012439429121"] = true,
        ["5569523548"] = true,
        ["92445809840331"] = true,
    }
    
    local allGuest1337s = getAllCharactersByName("Guest1337")
    for _, guest1337 in ipairs(allGuest1337s) do
        if not isLocalPlayer(guest1337) then
            local guest1337Hrp = guest1337:FindFirstChild("HumanoidRootPart")
            if guest1337Hrp then
                local distance = (guest1337Hrp.Position - myRoot.Position).Magnitude
                if distance <= detectionRange then
                    local hum = guest1337:FindFirstChildOfClass("Humanoid")
                    if hum then
                        local animator = hum:FindFirstChildOfClass("Animator")
                        if animator then
                            for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                                local animId = extractNumericAnimationId(track.Animation.AnimationId)
                                if animId and guest1337AllAnims[animId] then
                                    if blockdelay > 0 then
                                        task.wait(blockdelay)
                                    end
                                    
                                    pressKey(Enum.KeyCode.R)
                                    lastSpecialAbilityTime = now
                                    SendNotif("Sixer", "Blood Hunt used on Guest1337", 1)
                                    return
                                end
                            end
                        end
                    end
                end
            end
        end
    end
    
    local allShedletskies = getAllCharactersByName("Shedletsky")
    for _, shedletsky in ipairs(allShedletskies) do
        if not isLocalPlayer(shedletsky) then
            local shedletskyHrp = shedletsky:FindFirstChild("HumanoidRootPart")
            if shedletskyHrp then
                local distance = (shedletskyHrp.Position - myRoot.Position).Magnitude
                if distance <= 20 then
                    for _, sound in ipairs(shedletsky:GetDescendants()) do
                        if sound:IsA("Sound") and sound.IsPlaying then
                            local soundId = extractNumericSoundId(sound)
                            if soundId and triggerSoundIds[soundId] then
                                if blockdelay > 0 then
                                    task.wait(blockdelay)
                                end
                                
                                pressKey(Enum.KeyCode.R)
                                lastSpecialAbilityTime = now
                                SendNotif("Sixer", "Blood Hunt used on Shedletsky", 1)
                                return
                            end
                        end
                    end
                end
            end
        end
    end
    
    local allTwoTimes = getAllCharactersByName("TwoTime")
    for _, twotime in ipairs(allTwoTimes) do
        if not isLocalPlayer(twotime) then
            local twotimeHrp = twotime:FindFirstChild("HumanoidRootPart")
            if twotimeHrp then
                local distance = (twotimeHrp.Position - myRoot.Position).Magnitude
                if distance <= 30 then
                    for _, sound in ipairs(twotime:GetDescendants()) do
                        if sound:IsA("Sound") and sound.IsPlaying then
                            local soundId = extractNumericSoundId(sound)
                            if soundId and (soundId == "99820161736138" or soundId == "86710781315432") then
                                if blockdelay > 0 then
                                    task.wait(blockdelay)
                                end
                                
                                pressKey(Enum.KeyCode.R)
                                lastSpecialAbilityTime = now
                                SendNotif("Sixer", "Blood Hunt used on TwoTime", 1)
                                return
                            end
                        end
                    end
                end
            end
        end
    end
    
    local allChances = getAllCharactersByName("Chance")
    for _, chance in ipairs(allChances) do
        if not isLocalPlayer(chance) then
            local chanceHrp = chance:FindFirstChild("HumanoidRootPart")
            if chanceHrp then
                local distance = (chanceHrp.Position - myRoot.Position).Magnitude
                if distance <= 90 then
                    for _, sound in ipairs(chance:GetDescendants()) do
                        if sound:IsA("Sound") and sound.IsPlaying then
                            local soundId = extractNumericSoundId(sound)
                            if soundId and soundId == "139012439429121" then
                                if blockdelay > 0 then
                                    task.wait(blockdelay)
                                end
                                
                                pressKey(Enum.KeyCode.R)
                                lastSpecialAbilityTime = now
                                SendNotif("Sixer", "Blood Hunt used on Chance ability", 1)
                                return
                            end
                        end
                    end
                    
                    local hum = chance:FindFirstChildOfClass("Humanoid")
                    if hum then
                        local animator = hum:FindFirstChildOfClass("Animator")
                        if animator then
                            for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                                local animId = extractNumericAnimationId(track.Animation.AnimationId)
                                if animId and chanceAnimIds[animId] then
                                    if blockdelay > 0 then
                                        task.wait(blockdelay)
                                    end
                                    
                                    pressKey(Enum.KeyCode.R)
                                    lastSpecialAbilityTime = now
                                    SendNotif("Sixer", "Blood Hunt used on Chance animation", 1)
                                    return
                                end
                            end
                        end
                    end
                end
            end
        end
    end
    
    local allJaneDoes = getAllCharactersByName("JaneDoe")
    for _, janeDoe in ipairs(allJaneDoes) do
        if not isLocalPlayer(janeDoe) then
            local janeDoeHrp = janeDoe:FindFirstChild("HumanoidRootPart")
            if janeDoeHrp then
                local distance = (janeDoeHrp.Position - myRoot.Position).Magnitude
                if distance <= 20 then
                    for _, sound in ipairs(janeDoe:GetDescendants()) do
                        if sound:IsA("Sound") and sound.IsPlaying then
                            local soundId = extractNumericSoundId(sound)
                            if soundId and (soundId == "92445809840331" or soundId == "83851356262523") then
                                if blockdelay > 0 then
                                    task.wait(blockdelay)
                                end
                                
                                pressKey(Enum.KeyCode.R)
                                lastSpecialAbilityTime = now
                                SendNotif("Sixer", "Blood Hunt used on Jane Doe", 1)
                                return
                            end
                        end
                    end
                end
            end
        end
    end
end

-- ===== SPECIAL KILLER ACTIONS =====
local function checkSpecialKillerActions()
    local isSlasher, slasherChar = amISlasher()
    local isJohnDoe, johnDoeChar = amIJohnDoe()
    local is1x1x1x1, onexChar = amI1x1x1x1()
    
    if not (isSlasher or isJohnDoe or is1x1x1x1) then return end
    
    local now = tick()
    if now - lastSpecialAbilityTime < SPECIAL_ABILITY_COOLDOWN then return end
    
    local myChar = lp.Character
    if not myChar then return end
    
    local myRoot = myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return end
    
    local triggerSoundIds = {
        ["12222225"] = true,
        ["132596270805754"] = true,
        ["83851356262523"] = true,
        ["111910850942168"] = true,
        ["115267992424648"] = true,
        ["99820161736138"] = true,
        ["86710781315432"] = true,
        ["139012439429121"] = true,
        ["5569523548"] = true,
        ["92445809840331"] = true,
    }
    
    if isSlasher and ragingPaceAutoPunish then
        local allGuest1337s = getAllCharactersByName("Guest1337")
        
        for _, guest1337 in ipairs(allGuest1337s) do
            if not isLocalPlayer(guest1337) then
                local guest1337Hrp = guest1337:FindFirstChild("HumanoidRootPart")
                if guest1337Hrp then
                    local distance = (guest1337Hrp.Position - myRoot.Position).Magnitude
                    if distance <= detectionRange then
                        local hum = guest1337:FindFirstChildOfClass("Humanoid")
                        if hum then
                            local animator = hum:FindFirstChildOfClass("Animator")
                            if animator then
                                for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                                    local id = extractNumericAnimationId(track.Animation.AnimationId)
                                    if id and guest1337AllAnims[id] then
                                        if blockdelay > 0 then
                                            task.wait(blockdelay)
                                        end
                                        
                                        pressKey(Enum.KeyCode.R)
                                        lastSpecialAbilityTime = now
                                        SendNotif("Slasher", "Raging Pace used on Guest1337", 1)
                                        return true
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end
    end
    
    if is1x1x1x1 and onexOnexOnexOneAutoPunish then
        local allGuest1337s = getAllCharactersByName("Guest1337")
        
        for _, guest1337 in ipairs(allGuest1337s) do
            if not isLocalPlayer(guest1337) then
                local guest1337Hrp = guest1337:FindFirstChild("HumanoidRootPart")
                if guest1337Hrp then
                    local distance = (guest1337Hrp.Position - myRoot.Position).Magnitude
                    if distance <= detectionRange then
                        local hum = guest1337:FindFirstChildOfClass("Humanoid")
                        if hum then
                            local animator = hum:FindFirstChildOfClass("Animator")
                            if animator then
                                for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                                    local id = extractNumericAnimationId(track.Animation.AnimationId)
                                    if id and guest1337AllAnims[id] then
                                        if blockdelay > 0 then
                                            task.wait(blockdelay)
                                        end
                                        
                                        pressKey(Enum.KeyCode.Q)
                                        lastSpecialAbilityTime = now
                                        SendNotif("1x1x1x1", "Mass Infection used on Guest1337", 1)
                                        return true
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end
    end
    
    if isSlasher and ragingPaceAutoParry then
        local allGuest1337s = getAllCharactersByName("Guest1337")
        for _, guest1337 in ipairs(allGuest1337s) do
            if not isLocalPlayer(guest1337) then
                local guest1337Hrp = guest1337:FindFirstChild("HumanoidRootPart")
                if guest1337Hrp then
                    local distance = (guest1337Hrp.Position - myRoot.Position).Magnitude
                    if distance <= detectionRange then
                        local hum = guest1337:FindFirstChildOfClass("Humanoid")
                        if hum then
                            local animator = hum:FindFirstChildOfClass("Animator")
                            if animator then
                                for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                                    local animId = extractNumericAnimationId(track.Animation.AnimationId)
                                    if animId and guest1337PunchAnims[animId] then
                                        if blockdelay > 0 then
                                            task.wait(blockdelay)
                                        end
                                        
                                        pressKey(Enum.KeyCode.R)
                                        lastSpecialAbilityTime = now
                                        SendNotif("Slasher", "Raging Pace used on Guest1337 (punch)", 1)
                                        return true
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end
        
        local allShedletskies = getAllCharactersByName("Shedletsky")
        for _, shedletsky in ipairs(allShedletskies) do
            if not isLocalPlayer(shedletsky) then
                local shedletskyHrp = shedletsky:FindFirstChild("HumanoidRootPart")
                if shedletskyHrp then
                    local distance = (shedletskyHrp.Position - myRoot.Position).Magnitude
                    if distance <= 20 then
                        for _, sound in ipairs(shedletsky:GetDescendants()) do
                            if sound:IsA("Sound") and sound.IsPlaying then
                                local soundId = extractNumericSoundId(sound)
                                if soundId and triggerSoundIds[soundId] then
                                    if blockdelay > 0 then
                                        task.wait(blockdelay)
                                    end
                                    
                                    pressKey(Enum.KeyCode.R)
                                    lastSpecialAbilityTime = now
                                    SendNotif("Slasher", "Raging Pace used on Shedletsky", 1)
                                    return true
                                end
                            end
                        end
                    end
                end
            end
        end
        
        local allTwoTimes = getAllCharactersByName("TwoTime")
        for _, twotime in ipairs(allTwoTimes) do
            if not isLocalPlayer(twotime) then
                local twotimeHrp = twotime:FindFirstChild("HumanoidRootPart")
                if twotimeHrp then
                    local distance = (twotimeHrp.Position - myRoot.Position).Magnitude
                    if distance <= 30 then
                        for _, sound in ipairs(twotime:GetDescendants()) do
                            if sound:IsA("Sound") and sound.IsPlaying then
                                local soundId = extractNumericSoundId(sound)
                                if soundId and (soundId == "99820161736138" or soundId == "86710781315432") then
                                    if blockdelay > 0 then
                                        task.wait(blockdelay)
                                    end
                                    
                                    pressKey(Enum.KeyCode.R)
                                    lastSpecialAbilityTime = now
                                    SendNotif("Slasher", "Raging Pace used on TwoTime", 1)
                                    return true
                                end
                            end
                        end
                    end
                end
            end
        end
        
        local allChances = getAllCharactersByName("Chance")
        for _, chance in ipairs(allChances) do
            if not isLocalPlayer(chance) then
                local chanceHrp = chance:FindFirstChild("HumanoidRootPart")
                if chanceHrp then
                    local distance = (chanceHrp.Position - myRoot.Position).Magnitude
                    if distance <= 90 then
                        for _, sound in ipairs(chance:GetDescendants()) do
                            if sound:IsA("Sound") and sound.IsPlaying then
                                local soundId = extractNumericSoundId(sound)
                                if soundId and soundId == "139012439429121" then
                                    if blockdelay > 0 then
                                        task.wait(blockdelay)
                                    end
                                    
                                    pressKey(Enum.KeyCode.R)
                                    lastSpecialAbilityTime = now
                                    SendNotif("Slasher", "Raging Pace used on Chance ability", 1)
                                    return true
                                end
                            end
                        end
                        
                        local hum = chance:FindFirstChildOfClass("Humanoid")
                        if hum then
                            local animator = hum:FindFirstChildOfClass("Animator")
                            if animator then
                                for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                                    local animId = extractNumericAnimationId(track.Animation.AnimationId)
                                    if animId and chanceAnimIds[animId] then
                                        if blockdelay > 0 then
                                            task.wait(blockdelay)
                                        end
                                        
                                        pressKey(Enum.KeyCode.R)
                                        lastSpecialAbilityTime = now
                                        SendNotif("Slasher", "Raging Pace used on Chance animation", 1)
                                        return true
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end
        
        local allJaneDoes = getAllCharactersByName("JaneDoe")
        for _, janeDoe in ipairs(allJaneDoes) do
            if not isLocalPlayer(janeDoe) then
                local janeDoeHrp = janeDoe:FindFirstChild("HumanoidRootPart")
                if janeDoeHrp then
                    local distance = (janeDoeHrp.Position - myRoot.Position).Magnitude
                    if distance <= 20 then
                        for _, sound in ipairs(janeDoe:GetDescendants()) do
                            if sound:IsA("Sound") and sound.IsPlaying then
                                local soundId = extractNumericSoundId(sound)
                                if soundId and (soundId == "92445809840331" or soundId == "83851356262523") then
                                    if blockdelay > 0 then
                                        task.wait(blockdelay)
                                    end
                                    
                                    pressKey(Enum.KeyCode.R)
                                    lastSpecialAbilityTime = now
                                    SendNotif("Slasher", "Raging Pace used on Jane Doe", 1)
                                    return true
                                end
                            end
                        end
                    end
                end
            end
        end
    end
    
    if isJohnDoe and error404AutoParry then
        local allShedletskies = getAllCharactersByName("Shedletsky")
        for _, shedletsky in ipairs(allShedletskies) do
            if not isLocalPlayer(shedletsky) then
                local shedletskyHrp = shedletsky:FindFirstChild("HumanoidRootPart")
                if shedletskyHrp then
                    local distance = (shedletskyHrp.Position - myRoot.Position).Magnitude
                    if distance <= 20 then
                        for _, sound in ipairs(shedletsky:GetDescendants()) do
                            if sound:IsA("Sound") and sound.IsPlaying then
                                local soundId = extractNumericSoundId(sound)
                                if soundId and triggerSoundIds[soundId] then
                                    if blockdelay > 0 then
                                        task.wait(blockdelay)
                                    end
                                    
                                    pressKey(Enum.KeyCode.R)
                                    lastSpecialAbilityTime = now
                                    SendNotif("JohnDoe", "404 Error used on Shedletsky", 1)
                                    return true
                                end
                            end
                        end
                    end
                end
            end
        end
        
        local allTwoTimes = getAllCharactersByName("TwoTime")
        for _, twotime in ipairs(allTwoTimes) do
            if not isLocalPlayer(twotime) then
                local twotimeHrp = twotime:FindFirstChild("HumanoidRootPart")
                if twotimeHrp then
                    local distance = (twotimeHrp.Position - myRoot.Position).Magnitude
                    if distance <= 30 then
                        for _, sound in ipairs(twotime:GetDescendants()) do
                            if sound:IsA("Sound") and sound.IsPlaying then
                                local soundId = extractNumericSoundId(sound)
                                if soundId and (soundId == "99820161736138" or soundId == "86710781315432") then
                                    if blockdelay > 0 then
                                        task.wait(blockdelay)
                                    end
                                    
                                    pressKey(Enum.KeyCode.R)
                                    lastSpecialAbilityTime = now
                                    SendNotif("JohnDoe", "404 Error used on TwoTime", 1)
                                    return true
                                end
                            end
                        end
                    end
                end
            end
        end
        
        local allChances = getAllCharactersByName("Chance")
        for _, chance in ipairs(allChances) do
            if not isLocalPlayer(chance) then
                local chanceHrp = chance:FindFirstChild("HumanoidRootPart")
                if chanceHrp then
                    local distance = (chanceHrp.Position - myRoot.Position).Magnitude
                    if distance <= 90 then
                        for _, sound in ipairs(chance:GetDescendants()) do
                            if sound:IsA("Sound") and sound.IsPlaying then
                                local soundId = extractNumericSoundId(sound)
                                if soundId and soundId == "139012439429121" then
                                    if blockdelay > 0 then
                                        task.wait(blockdelay)
                                    end
                                    
                                    pressKey(Enum.KeyCode.R)
                                    lastSpecialAbilityTime = now
                                    SendNotif("JohnDoe", "404 Error used on Chance ability", 1)
                                    return true
                                end
                            end
                        end
                        
                        local hum = chance:FindFirstChildOfClass("Humanoid")
                        if hum then
                            local animator = hum:FindFirstChildOfClass("Animator")
                            if animator then
                                for _, track in ipairs(animator:GetPlayingAnimationTracks()) do
                                    local animId = extractNumericAnimationId(track.Animation.AnimationId)
                                    if animId and chanceAnimIds[animId] then
                                        if blockdelay > 0 then
                                            task.wait(blockdelay)
                                        end
                                        
                                        pressKey(Enum.KeyCode.R)
                                        lastSpecialAbilityTime = now
                                        SendNotif("JohnDoe", "404 Error used on Chance animation", 1)
                                        return true
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end
        
        local allJaneDoes = getAllCharactersByName("JaneDoe")
        for _, janeDoe in ipairs(allJaneDoes) do
            if not isLocalPlayer(janeDoe) then
                local janeDoeHrp = janeDoe:FindFirstChild("HumanoidRootPart")
                if janeDoeHrp then
                    local distance = (janeDoeHrp.Position - myRoot.Position).Magnitude
                    if distance <= 20 then
                        for _, sound in ipairs(janeDoe:GetDescendants()) do
                            if sound:IsA("Sound") and sound.IsPlaying then
                                local soundId = extractNumericSoundId(sound)
                                if soundId and (soundId == "92445809840331" or soundId == "83851356262523") then
                                    if blockdelay > 0 then
                                        task.wait(blockdelay)
                                    end
                                    
                                    pressKey(Enum.KeyCode.R)
                                    lastSpecialAbilityTime = now
                                    SendNotif("JohnDoe", "404 Error used on Jane Doe", 1)
                                    return true
                                end
                            end
                        end
                    end
                end
            end
        end
    end
    
    return false
end

-- ===== SPECIAL KILLER STATUS =====
local function checkSpecialKillerStatus()
    local character = lp.Character
    if not character then 
        if isPlayingAsSpecialKiller then
            isPlayingAsSpecialKiller = false
            currentKillerType = nil
            facingCheckEnabled = originalFacingCheckState
        end
        return false
    end
    
    local allSlashers = getAllCharactersByName("Slasher")
    local allJohnDoes = getAllCharactersByName("JohnDoe")
    local allOnexOnexOnexOnes = getAllCharactersByName("1x1x1x1")
    
    for _, slasher in ipairs(allSlashers) do
        local usernameAttr = slasher:GetAttribute("Username")
        if usernameAttr and usernameAttr == lp.Name then
            if not isPlayingAsSpecialKiller or currentKillerType ~= "Slasher" then
                isPlayingAsSpecialKiller = true
                currentKillerType = "Slasher"
                originalFacingCheckState = facingCheckEnabled
                facingCheckEnabled = false
            end
            return true
        end
    end
    
    for _, johnDoe in ipairs(allJohnDoes) do
        local usernameAttr = johnDoe:GetAttribute("Username")
        if usernameAttr and usernameAttr == lp.Name then
            if not isPlayingAsSpecialKiller or currentKillerType ~= "JohnDoe" then
                isPlayingAsSpecialKiller = true
                currentKillerType = "JohnDoe"
                originalFacingCheckState = facingCheckEnabled
                facingCheckEnabled = false
            end
            return true
        end
    end
    
    for _, onexone in ipairs(allOnexOnexOnexOnes) do
        local usernameAttr = onexone:GetAttribute("Username")
        if usernameAttr and usernameAttr == lp.Name then
            if not isPlayingAsSpecialKiller or currentKillerType ~= "1x1x1x1" then
                isPlayingAsSpecialKiller = true
                currentKillerType = "1x1x1x1"
                originalFacingCheckState = facingCheckEnabled
                facingCheckEnabled = false
            end
            return true
        end
    end
    
    if isPlayingAsSpecialKiller then
        isPlayingAsSpecialKiller = false
        currentKillerType = nil
        facingCheckEnabled = originalFacingCheckState
    end
    
    return false
end

-- ===== VISUAL RANGE =====
local lastVisualUpdate = 0
local VISUAL_UPDATE_COOLDOWN = 0.5

local function safeDestroyCircle(circle)
    if not circle then return end
    pcall(function()
        if circle.Parent then
            circle:Destroy()
        end
    end)
end

local function updateDetectionRangeVisuals()
    local now = tick()
    if now - lastVisualUpdate < VISUAL_UPDATE_COOLDOWN then return end
    lastVisualUpdate = now
    
    for _, circle in pairs(detectionCircles) do
        safeDestroyCircle(circle)
    end
    detectionCircles = {}
    
    for _, circle in pairs(survivorCircles) do
        safeDestroyCircle(circle)
    end
    survivorCircles = {}
    
    if not killerCirclesVisible then return end
    
    local myChar = lp.Character
    local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return end
    
    if KillersFolder then
        for _, killer in ipairs(KillersFolder:GetChildren()) do
            local killerHrp = killer:FindFirstChild("HumanoidRootPart")
            if killerHrp and not killerHrp:FindFirstChild("DetectionCircle") then
                local usernameAttr = killer:GetAttribute("Username")
                if not (usernameAttr and usernameAttr == lp.Name) then
                    local distance = (killerHrp.Position - myRoot.Position).Magnitude
                    local inRange = distance <= detectionRange
                    
                    pcall(function()
                        local circle = Instance.new("CylinderHandleAdornment")
                        circle.Name = "DetectionCircle"
                        circle.Adornee = killerHrp
                        circle.Color3 = inRange and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(255, 165, 0)
                        circle.AlwaysOnTop = true
                        circle.ZIndex = 1
                        circle.Transparency = 0.6
                        circle.Radius = detectionRange
                        circle.Height = 0.12
                        
                        local yOffset = -(killerHrp.Size.Y / 2 + 0.05)
                        circle.CFrame = CFrame.new(0, yOffset, 0) * CFrame.Angles(math.rad(90), 0, 0)
                        
                        circle.Parent = killerHrp
                        detectionCircles[killer] = circle
                    end)
                end
            end
        end
    end
end

-- ===== STUN IMMUNE VISUAL FUNCTIONS =====
local function updateStunImmuneIndicator(killer)
    local hrp = killer:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    
    local usernameAttr = killer:GetAttribute("Username")
    if usernameAttr and usernameAttr == lp.Name then
        return
    end
    
    local isStunImmune = false
    
    if hrp:FindFirstChild("InvincibleFX") then
        isStunImmune = true
    end
    
    if hrp:FindFirstChild("StunProtection") then
        isStunImmune = true
    end
    
    local existingBillboard = hrp:FindFirstChild("StunImmuneBillboard")
    
    if isStunImmune then
        if not existingBillboard then
            local billboard = Instance.new("BillboardGui")
            billboard.Name = "StunImmuneBillboard"
            billboard.Size = UDim2.new(0, 120, 0, 30)
            billboard.StudsOffset = Vector3.new(0, 2.5, 0)
            billboard.AlwaysOnTop = true
            billboard.Parent = hrp
            
            local textLabel = Instance.new("TextLabel")
            textLabel.Size = UDim2.new(1, 0, 1, 0)
            textLabel.BackgroundTransparency = 1
            textLabel.Text = "⚡ STUN IMMUNE ⚡"
            textLabel.TextColor3 = Color3.fromRGB(255, 255, 0)
            textLabel.TextStrokeTransparency = 0
            textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
            textLabel.TextScaled = true
            textLabel.Font = Enum.Font.GothamBold
            textLabel.Parent = billboard
        end
    else
        if existingBillboard then
            existingBillboard:Destroy()
        end
    end
end

local function scanAllKillersForStunImmune()
    if not stunImmuneEnabled then return end
    
    if KillersFolder then
        for _, killer in ipairs(KillersFolder:GetChildren()) do
            updateStunImmuneIndicator(killer)
        end
    end
end

local function startStunImmuneLoop()
    if stunImmuneLoop then
        task.cancel(stunImmuneLoop)
        stunImmuneLoop = nil
    end
    
    if not stunImmuneEnabled then return end
    
    stunImmuneLoop = task.spawn(function()
        while stunImmuneEnabled do
            task.wait(0.5)
            scanAllKillersForStunImmune()
        end
    end)
end

local function setupStunImmuneListeners()
    if not KillersFolder then return end
    
    KillersFolder.ChildAdded:Connect(function(newKiller)
        task.wait(0.5)
        if stunImmuneEnabled then
            updateStunImmuneIndicator(newKiller)
            
            local hrp = newKiller:FindFirstChild("HumanoidRootPart")
            if hrp then
                local function onChildAdded(child)
                    if child.Name == "InvincibleFX" or child.Name == "StunProtection" then
                        updateStunImmuneIndicator(newKiller)
                    end
                end
                
                local function onChildRemoved(child)
                    if child.Name == "InvincibleFX" or child.Name == "StunProtection" then
                        updateStunImmuneIndicator(newKiller)
                    end
                end
                
                hrp.ChildAdded:Connect(onChildAdded)
                hrp.ChildRemoved:Connect(onChildRemoved)
            end
        end
    end)
    
    KillersFolder.ChildRemoved:Connect(function(killer)
        local hrp = killer:FindFirstChild("HumanoidRootPart")
        if hrp then
            local billboard = hrp:FindFirstChild("StunImmuneBillboard")
            if billboard then
                billboard:Destroy()
            end
        end
    end)
end

-- ===== INPUT HOOK FOR AIMBOT =====
local function setupInputHook()
    UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then return end
        
        local myTeam, myCharacter = getMyCharacterInfo()
        local isKiller = (myTeam == "Killer")
        local isShedletsky = (myTeam == "Survivor" and myCharacter == "Shedletsky")
        local isChance = (myTeam == "Survivor" and myCharacter == "Chance")
        local isTwoTime = (myTeam == "Survivor" and myCharacter == "TwoTime")
        local isElliot = (myTeam == "Survivor" and myCharacter == "Elliot")
        local isJaneDoe = (myTeam == "Survivor" and myCharacter == "JaneDoe")
        local isGuest1337 = (myTeam == "Survivor" and myCharacter == "Guest1337")
        local isSlasher = (myTeam == "Killer" and myCharacter == "Slasher")
        local isJohnDoe = (myTeam == "Killer" and myCharacter == "JohnDoe")
        local is1x1x1x1 = (myTeam == "Killer" and myCharacter == "1x1x1x1")
        local isSixer = (myTeam == "Killer" and myCharacter == "Sixer")
        local isc00lkidd = (myTeam == "Killer" and myCharacter == "c00lkidd")
        local isNoli = (myTeam == "Killer" and myCharacter == "Noli")
        
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            if not aimbotEnabled then return end
            
            if myTeam == "Killer" then
                local abilityContainer = lp.PlayerGui.MainUI.AbilityContainer
                if abilityContainer then
                    local cooldownTime = nil
                    
                    if myCharacter == "Slasher" then
                        local slash = abilityContainer:FindFirstChild("Slash")
                        if slash then cooldownTime = slash:FindFirstChild("CooldownTime") end
                    elseif myCharacter == "c00lkidd" then
                        local punch = abilityContainer:FindFirstChild("Punch")
                        if punch then cooldownTime = punch:FindFirstChild("CooldownTime") end
                    elseif myCharacter == "JohnDoe" then
                        local slash = abilityContainer:FindFirstChild("Slash")
                        if slash then cooldownTime = slash:FindFirstChild("CooldownTime") end
                    elseif myCharacter == "Noli" then
                        local stab = abilityContainer:FindFirstChild("Stab")
                        if stab then cooldownTime = stab:FindFirstChild("CooldownTime") end
                    elseif myCharacter == "1x1x1x1" then
                        local slash = abilityContainer:FindFirstChild("Slash")
                        if slash then cooldownTime = slash:FindFirstChild("CooldownTime") end
                    elseif myCharacter == "Sixer" then
                        local carvingSlash = abilityContainer:FindFirstChild("Carving Slash")
                        if carvingSlash then cooldownTime = carvingSlash:FindFirstChild("CooldownTime") end
                    end
                    
                    if cooldownTime then
                        local contentText = cooldownTime.ContentText
                        if contentText ~= "" and contentText ~= "0" and contentText ~= "0.0" and contentText ~= "0.1" then
                            return
                        end
                    end
                end
            end
            
            if myCharacter == "Sixer" then
                local charges = getSixerCarvingSlashCharges()
                if charges >= 1 then
                    startGeneralAimbot(2.0)
                else
                    startGeneralAimbot(0.8)
                end
            elseif myTeam == "Killer" then
                startGeneralAimbot(0.65)
            end
        end
        
        if input.UserInputType == Enum.UserInputType.Keyboard and input.KeyCode == Enum.KeyCode.Q then
            if not aimbotEnabled then return end
            
            if isTwoTime then
                if not isTwoTimeDaggerReady() then
                    SendNotif("TwoTime", "Dagger is on cooldown!", 1)
                    return
                end
                
                local targetKiller = getNearestKiller()
                if not targetKiller then
                    SendNotif("TwoTime", "No killer found!", 1)
                    return
                end
                
                if isCrouching() then
                    SendNotif("TwoTime", "DASH STAB!", 1)
                    startDashStab(targetKiller)
                else
                    aimbotActive = true
                    aimbotCurrentTarget = targetKiller
                    
                    if faceSameSideAsKiller then
                        SendNotif("TwoTime", "Backstab mode!", 1)
                    else
                        SendNotif("TwoTime", "Normal stab!", 1)
                    end
                    startNormalStab(targetKiller)
                end
            elseif isNoli then
                startGeneralAimbot(2.5)
            elseif isSixer then
                startGeneralAimbot(1.45)
            elseif isJohnDoe then
                startGeneralAimbot(4.5)
            elseif isShedletsky then
                startGeneralAimbot(1.1)
            elseif is1x1x1x1 then
                startGeneralAimbot(2.1)
            elseif isSlasher then
                startGeneralAimbot(0.95)
            elseif isElliot then
                startGeneralAimbot(0.5)
            end
        end
        
        if input.UserInputType == Enum.UserInputType.Keyboard and input.KeyCode == Enum.KeyCode.E then
            if not aimbotEnabled then return end
            
            if isc00lkidd then
                startGeneralAimbot(0.6)
            elseif isSixer then
                startGeneralAimbot(1.0)
            elseif isChance then
                startGeneralAimbot(1.1)
            elseif is1x1x1x1 then
                startGeneralAimbot(1.2)
            elseif isSlasher then
                startGeneralAimbot(0.5)
            elseif isJaneDoe then
                startGeneralAimbot(0.9)
            end
        end
        
        if input.UserInputType == Enum.UserInputType.Keyboard and input.KeyCode == Enum.KeyCode.R then
            if not aimbotEnabled then return end
            if isGuest1337 then
                startGeneralAimbot(0.95)
            end
        end
    end)
end

-- ===== ABILITY COOLDOWN TRACKER FUNCTIONS =====
local function formatCooldownTime(remainingSeconds)
    if remainingSeconds <= 0 then
        return "0.0"
    elseif remainingSeconds < DECIMAL_START then
        return string.format("%.1f", remainingSeconds)
    else
        return tostring(math.floor(remainingSeconds))
    end
end

local function getAbilityName(survivorName)
    if survivorName == "Shedletsky" then
        return "Sword"
    elseif survivorName == "TwoTime" then
        return "Dagger"
    elseif survivorName == "Chance" then
        return "Gun"
    elseif survivorName == "JaneDoe" then
        return "Hatchet"
    elseif survivorName == "Guest1337" then
        return "Block"
    elseif survivorName == "Elliot" then
        return "Pizza"
    end
    return "Ability"
end

local function getCooldown(survivorName)
    if survivorName == "Shedletsky" then
        return SHEDLETSKY_COOLDOWN
    elseif survivorName == "TwoTime" then
        return TWOTIME_COOLDOWN
    elseif survivorName == "Chance" then
        return CHANCE_COOLDOWN
    elseif survivorName == "JaneDoe" then
        return JANEDOE_COOLDOWN
    elseif survivorName == "Guest1337" then
        return GUEST1337_COOLDOWN
    elseif survivorName == "Elliot" then
        return ELLIOT_COOLDOWN
    end
    return 40
end

local function createCooldownDisplay(survivor, cooldownEndTime, abilityName)
    if activeCooldownTimers[survivor] then
        local existing = activeCooldownTimers[survivor]
        if existing.billboard then
            pcall(function() existing.billboard:Destroy() end)
        end
        if existing.connection then
            existing.connection:Disconnect()
        end
        activeCooldownTimers[survivor] = nil
    end
    
    local hrp = survivor:FindFirstChild("HumanoidRootPart") or survivor:FindFirstChild("Head") or survivor:FindFirstChild("Torso")
    if not hrp then return end
    
    local billboard = Instance.new("BillboardGui")
    billboard.Name = "AbilityCooldown"
    billboard.Size = UDim2.new(0, 120, 0, 40)
    billboard.StudsOffset = Vector3.new(0, 3, 0)
    billboard.AlwaysOnTop = true
    billboard.Parent = hrp
    
    local textLabel = Instance.new("TextLabel")
    textLabel.Name = "CooldownText"
    textLabel.Size = UDim2.new(1, 0, 1, 0)
    textLabel.BackgroundTransparency = 1
    textLabel.Text = abilityName .. ": " .. formatCooldownTime(getCooldown(survivor.Name))
    textLabel.TextStrokeTransparency = 0
    textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
    textLabel.TextScaled = true
    textLabel.Font = Enum.Font.GothamBold
    textLabel.Parent = billboard
    
    local timerData = {
        endTime = cooldownEndTime,
        billboard = billboard,
        textLabel = textLabel,
        abilityName = abilityName,
        survivorName = survivor.Name,
        connection = nil
    }
    
    local connection
    connection = RunService.Heartbeat:Connect(function()
        if not survivor or not survivor.Parent then
            if connection then connection:Disconnect() end
            if billboard then pcall(function() billboard:Destroy() end) end
            activeCooldownTimers[survivor] = nil
            return
        end
        
        local currentTime = tick()
        local remaining = timerData.endTime - currentTime
        
        if remaining <= 0.1 then
            if connection then connection:Disconnect() end
            if billboard then pcall(function() billboard:Destroy() end) end
            activeCooldownTimers[survivor] = nil
        else
            local formattedTime = formatCooldownTime(remaining)
            textLabel.Text = timerData.abilityName .. ": " .. formattedTime
            
            if remaining < 5 then
                textLabel.TextColor3 = Color3.fromRGB(255, 0, 0)
            elseif remaining < 10 then
                textLabel.TextColor3 = Color3.fromRGB(255, 165, 0)
            else
                textLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
        end
    end)
    
    timerData.connection = connection
    activeCooldownTimers[survivor] = timerData
end

local function listenForCooldownAbility(survivor)
    if not survivor then return end
    
    -- Don't track abilities on yourself
    if isLocalPlayer(survivor) then
        return
    end
    
    if survivor:GetAttribute("CooldownListener") then
        return
    end
    
    survivor:SetAttribute("CooldownListener", true)
    local survivorName = survivor.Name
    
    local function onAbilityDetected(cooldown)
        local cooldownEndTime = tick() + cooldown
        local abilityName = getAbilityName(survivorName)
        createCooldownDisplay(survivor, cooldownEndTime, abilityName)
    end
    
    local function onGuest1337BlockDetected()
        local now = tick()
        local lastTrigger = lastGuest1337Trigger[survivor] or 0
        if now - lastTrigger >= GUEST1337_ANTI_SPAM_TIME then
            lastGuest1337Trigger[survivor] = now
            onAbilityDetected(GUEST1337_COOLDOWN)
        end
    end
    
    local function onChanceAbilityDetected()
        local now = tick()
        local lastTrigger = lastChanceTrigger[survivor] or 0
        if now - lastTrigger >= CHANCE_ANTI_SPAM_TIME then
            lastChanceTrigger[survivor] = now
            onAbilityDetected(CHANCE_COOLDOWN)
        end
    end
    
    local function onSoundPlayed(sound)
        if not sound.IsPlaying then return end
        local soundId = extractNumericSoundId(sound)
        
        if survivorName == "Shedletsky" and soundId and shedletskySwingSounds[soundId] then
            onAbilityDetected(SHEDLETSKY_COOLDOWN)
        elseif survivorName == "TwoTime" and soundId and twotimeSounds[soundId] then
            onAbilityDetected(TWOTIME_COOLDOWN)
        elseif survivorName == "Chance" and soundId and chanceSounds[soundId] then
            onChanceAbilityDetected()
        elseif survivorName == "JaneDoe" and soundId and janeDoeSounds[soundId] then
            onAbilityDetected(JANEDOE_COOLDOWN)
        end
    end
    
    survivor.DescendantAdded:Connect(function(descendant)
        if descendant:IsA("Sound") then
            if descendant.IsPlaying then
                onSoundPlayed(descendant)
            end
            descendant.Played:Connect(function()
                onSoundPlayed(descendant)
            end)
        end
    end)
    
    local humanoid = survivor:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local function setupAnimator(animator)
            if not animator then return end
            
            animator.AnimationPlayed:Connect(function(animationTrack)
                local animId = extractNumericAnimationId(animationTrack.Animation.AnimationId)
                
                if survivorName == "Chance" and animId and chanceAnimIds[animId] then
                    onChanceAbilityDetected()
                elseif survivorName == "Guest1337" and animId and guest1337AllAnims[animId] then
                    onGuest1337BlockDetected()
                elseif survivorName == "Elliot" and animId and elliotThrowAnimation[animId] then
                    onAbilityDetected(ELLIOT_COOLDOWN)
                end
            end)
        end
        
        local animator = humanoid:FindFirstChildOfClass("Animator")
        if animator then
            setupAnimator(animator)
        end
        
        humanoid.ChildAdded:Connect(function(child)
            if child:IsA("Animator") then
                setupAnimator(child)
            end
        end)
    end
end

local function scanForAllCooldownSurvivors()
    local survivorsToTrack = {"Shedletsky", "TwoTime", "Chance", "JaneDoe", "Guest1337", "Elliot"}
    for _, survivorName in ipairs(survivorsToTrack) do
        local survivors = getAllCharactersByName(survivorName)
        for _, survivor in ipairs(survivors) do
            listenForCooldownAbility(survivor)
        end
    end
end

local function watchForNewCooldownSurvivors()
    local playersFolder = Workspace:FindFirstChild("Players")
    if not playersFolder then return end
    
    local survivorsFolder = playersFolder:FindFirstChild("Survivors")
    if not survivorsFolder then return end
    
    survivorsFolder.ChildAdded:Connect(function(child)
        local survivorsToTrack = {"Shedletsky", "TwoTime", "Chance", "JaneDoe", "Guest1337", "Elliot"}
        for _, survivorName in ipairs(survivorsToTrack) do
            if child.Name == survivorName then
                task.wait(0.5)
                listenForCooldownAbility(child)
                break
            end
        end
    end)
end

-- ===== DUSEKKAR SILENT AIM FUNCTIONS =====
local function isPlasmaBeamOnCooldown()
    local success, cooldownText = pcall(function()
        return lp.PlayerGui.MainUI.AbilityContainer.PlasmaBeam.CooldownTime.Text
    end)
    
    if success then
        return cooldownText ~= "" and cooldownText ~= "0"
    end
    return true
end

local function firePlasmaBeam()
    if not testRemote then return false end
    
    if isPlasmaBeamOnCooldown() then
        return false
    end
    
    local args = {
        "UseActorAbility",
        {
            buffer.fromstring("\003\n\000\000\000PlasmaBeam")
        }
    }
    
    local success = pcall(function()
        testRemote:FireServer(unpack(args))
    end)
    
    if success then
        return true
    end
    return false
end

local function getSurvivorClosestToCursor()
    local myChar = lp.Character
    if not myChar then return nil end
    
    local myRoot = myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return nil end
    
    local mousePosition = UserInputService:GetMouseLocation()
    
    local validTargets = {}
    
    for _, survivor in ipairs(SurvivorsFolder:GetChildren()) do
        local usernameAttr = survivor:GetAttribute("Username")
        if not (usernameAttr and usernameAttr == lp.Name) then
            local humanoid = survivor:FindFirstChildOfClass("Humanoid")
            if humanoid and humanoid.Health > 0 then
                local targetHrp = survivor:FindFirstChild("HumanoidRootPart")
                if targetHrp then
                    local screenPos, onScreen = Camera:WorldToViewportPoint(targetHrp.Position)
                    
                    if onScreen then
                        local screenVector = Vector2.new(screenPos.X, screenPos.Y)
                        local distanceToCursor = (screenVector - mousePosition).Magnitude
                        
                        table.insert(validTargets, {
                            survivor = survivor,
                            screenDistance = distanceToCursor
                        })
                    end
                end
            end
        end
    end
    
    table.sort(validTargets, function(a, b)
        return a.screenDistance < b.screenDistance
    end)
    
    if #validTargets > 0 then
        return validTargets[1].survivor
    end
    
    local nearestByDistance = nil
    local nearestDist = math.huge
    
    for _, survivor in ipairs(SurvivorsFolder:GetChildren()) do
        local usernameAttr = survivor:GetAttribute("Username")
        if not (usernameAttr and usernameAttr == lp.Name) then
            local humanoid = survivor:FindFirstChildOfClass("Humanoid")
            if humanoid and humanoid.Health > 0 then
                local targetHrp = survivor:FindFirstChild("HumanoidRootPart")
                if targetHrp then
                    local distance = (targetHrp.Position - myRoot.Position).Magnitude
                    if distance < nearestDist then
                        nearestDist = distance
                        nearestByDistance = survivor
                    end
                end
            end
        end
    end
    
    return nearestByDistance
end

local function getNearestKillerForDusekkar()
    local myChar = lp.Character
    if not myChar then return nil end
    
    local myRoot = myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return nil end
    
    local nearestKiller = nil
    local nearestDistance = math.huge
    
    for _, killer in ipairs(KillersFolder:GetChildren()) do
        local usernameAttr = killer:GetAttribute("Username")
        if not (usernameAttr and usernameAttr == lp.Name) then
            local targetHrp = killer:FindFirstChild("HumanoidRootPart")
            if targetHrp then
                local humanoid = killer:FindFirstChildOfClass("Humanoid")
                if humanoid and humanoid.Health > 0 then
                    local distance = (targetHrp.Position - myRoot.Position).Magnitude
                    if distance < nearestDistance then
                        nearestDistance = distance
                        nearestKiller = killer
                    end
                end
            end
        end
    end
    
    return nearestKiller
end

local function stopDusekkar()
    dusekkarActive = false
    currentDusekkarTarget = nil
    silentAimActive = false
    
    if currentTargetSphere then
        pcall(function() currentTargetSphere:Destroy() end)
        currentTargetSphere = nil
    end
    if currentSphereConnection then
        pcall(function() currentSphereConnection:Disconnect() end)
        currentSphereConnection = nil
    end
    
    if dusekkarTimer then
        task.cancel(dusekkarTimer)
        dusekkarTimer = nil
    end
end

local function setupSilentAim()
    local success, module = pcall(function()
        return require(ReplicatedStorage:FindFirstChild("Systems"):FindFirstChild("Player"):FindFirstChild("Miscellaneous"):FindFirstChild("GetPlayerMousePosition"))
    end)
    
    if not success then
        success, module = pcall(function()
            return require(ReplicatedStorage.Systems.Player.Miscellaneous.GetPlayerMousePosition)
        end)
    end
    
    if success and module then
        mousePositionModule = module
        originalGetMousePos = module.GetMousePos
        
        local function hookedGetMousePos(self, ...)
            local args = {...}
            
            if silentAimActive and currentTargetSphere then
                return currentTargetSphere.Position
            end
            
            return originalGetMousePos(self, ...)
        end
        
        module.GetMousePos = hookedGetMousePos
        return true
    end
    return false
end

local function restoreSilentAim()
    if mousePositionModule and originalGetMousePos then
        mousePositionModule.GetMousePos = originalGetMousePos
    end
end

local function useSilentAim()
    if not dusekkarEnabled then 
        return false 
    end
    
    if not amIDusekkar() then 
        return false 
    end
    
    local target = nil
    if dusekkarTargetMode == "Survivors" then
        target = getSurvivorClosestToCursor()
        if target then
            SendNotif("Dusekkar", "Targeting survivor: " .. target.Name, 1.5)
        end
    else
        target = getNearestKillerForDusekkar()
        if target then
            SendNotif("Dusekkar", "Targeting killer: " .. target.Name, 1.5)
        end
    end
    
    if not target then 
        SendNotif("Dusekkar", "No target found!", 1)
        return false 
    end
    
    if isPlasmaBeamOnCooldown() then 
        SendNotif("Dusekkar", "Plasma Beam on cooldown!", 1)
        return false 
    end
    
    currentDusekkarTarget = target
    silentAimActive = true
    dusekkarActive = true
    
    local oldPrediction = aimbotPredictionMultiplier
    aimbotPredictionMultiplier = dusekkarPredictionMultiplier
    createTargetSphere(target, Color3.fromRGB(0, 170, 255))
    aimbotPredictionMultiplier = oldPrediction
    
    local success = firePlasmaBeam()
    
    if success then
        if dusekkarTimer then
            task.cancel(dusekkarTimer)
        end
        
        dusekkarTimer = task.delay(2.5, function()
            stopDusekkar()
        end)
        
        return true
    else
        stopDusekkar()
        return false
    end
end

-- ===== MAIN LOOP =====
local mainLoopConn = RunService.Heartbeat:Connect(function()
    pcall(checkSpecialKillerStatus)
    
    if isPlayingAsSpecialKiller then
        pcall(checkSpecialKillerActions)
    end
    
    pcall(checkSixerBloodHuntParry)
    
    if autoSwitchBlockCharge and not isPlayingAsSpecialKiller then
        pcall(checkAndSwitchBlockType)
    end
    
    if autoBlockForGuest and not isPlayingAsSpecialKiller then
        pcall(checkForAttackSounds)
    end
    
    if autoCloneFor7n7 and not isPlayingAsSpecialKiller then
        pcall(checkForCloneTrigger)
    end
    
    if killerCirclesVisible then
        pcall(updateDetectionRangeVisuals)
    end
end)

-- ===== UI =====
local Window = Rayfield:CreateWindow({
    Name = "Pillager Cheats",
    LoadingTitle = "Loading...",
    LoadingSubtitle = "All Features Included",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "PillagerCheats",
        FileName = "Settings"
    },
    Discord = {Enabled = false},
    KeySystem = false
})

-- ===== AUTO BLOCK TAB =====
local AutoBlockTab = Window:CreateTab("Auto Block", 4483362458)

GuestToggle = AutoBlockTab:CreateToggle({
    Name = "1. Guest Auto Block (All killers) - Presses Q",
    CurrentValue = false,
    Flag = "GuestAutoBlockToggle",
    Callback = function(Value)
        local isGuest1337 = amIGuest1337()
        if isGuest1337 then
            autoBlockForGuest = Value
            SendNotif("Guest Auto Block", Value and "Enabled" or "Disabled", 1)
        end
    end
})

AutoBlockTab:CreateKeybind({
    Name = "Guest Auto Block Keybind",
    CurrentKeybind = "T",
    HoldToInteract = false,
    Flag = "GuestAutoBlockKey",
    Callback = function()
        local isGuest1337 = amIGuest1337()
        if isGuest1337 then
            local newValue = not autoBlockForGuest
            GuestToggle:Set(newValue)
            autoBlockForGuest = newValue
            SendNotif("Guest Auto Block", newValue and "Enabled" or "Disabled", 1)
        end
    end
})

SevenNToggle = AutoBlockTab:CreateToggle({
    Name = "2. 7n7 Auto Clone + Rotation - Presses Q",
    CurrentValue = false,
    Flag = "SevenNAutoCloneToggle",
    Callback = function(Value)
        local is7n7 = amI7n7()
        if is7n7 then
            autoCloneFor7n7 = Value
            SendNotif("7n7 Auto Clone", Value and "Enabled" or "Disabled", 1)
        end
    end
})

AutoBlockTab:CreateKeybind({
    Name = "7n7 Auto Clone Keybind",
    CurrentKeybind = "Y",
    HoldToInteract = false,
    Flag = "SevenNAutoCloneKey",
    Callback = function()
        local is7n7 = amI7n7()
        if is7n7 then
            local newValue = not autoCloneFor7n7
            SevenNToggle:Set(newValue)
            autoCloneFor7n7 = newValue
            SendNotif("7n7 Auto Clone", newValue and "Enabled" or "Disabled", 1)
        end
    end
})

AutoBlockTab:CreateToggle({
    Name = "3. Raging Pace Auto Parry (Slasher) - Presses R",
    CurrentValue = false,
    Flag = "RagingPaceAutoParry",
    Callback = function(Value)
        ragingPaceAutoParry = Value
        SendNotif("Slasher", "Raging Pace Auto Parry " .. (Value and "Enabled" or "Disabled"), 1)
    end
})

AutoBlockTab:CreateToggle({
    Name = "4. Error 404 Auto Parry (JohnDoe) - Presses R",
    CurrentValue = false,
    Flag = "Error404AutoParry",
    Callback = function(Value)
        error404AutoParry = Value
        SendNotif("JohnDoe", "Error 404 Auto Parry " .. (Value and "Enabled" or "Disabled"), 1)
    end
})

AutoBlockTab:CreateToggle({
    Name = "5. Blood Hunt Auto Parry (Sixer) - Presses R",
    CurrentValue = false,
    Flag = "BloodHuntAutoParry",
    Callback = function(Value)
        bloodHuntAutoParry = Value
        SendNotif("Sixer", "Blood Hunt Auto Parry " .. (Value and "Enabled" or "Disabled"), 1)
    end
})

AutoBlockTab:CreateToggle({
    Name = "6. Raging Pace Auto Punish (Slasher) - Presses R",
    CurrentValue = false,
    Flag = "RagingPaceAutoPunish",
    Callback = function(Value)
        ragingPaceAutoPunish = Value
        SendNotif("Slasher", "Raging Pace Auto Punish " .. (Value and "Enabled" or "Disabled"), 1)
    end
})

AutoBlockTab:CreateToggle({
    Name = "7. Mass Infection Auto Punish (1x1x1x1) - Presses Q",
    CurrentValue = false,
    Flag = "OnexOnexOnexOneAutoPunish",
    Callback = function(Value)
        onexOnexOnexOneAutoPunish = Value
        SendNotif("1x1x1x1", "Mass Infection Auto Punish " .. (Value and "Enabled" or "Disabled"), 1)
    end
})

AutoBlockTab:CreateSlider({
    Name = "Block Chance",
    Range = {0, 100},
    Increment = 5,
    Suffix = "%",
    CurrentValue = 100,
    Flag = "BlockChance",
    Callback = function(Value)
        blockChance = Value
    end
})

AutoBlockTab:CreateToggle({
    Name = "Auto Switch Block/Charge",
    CurrentValue = false,
    Flag = "AutoSwitchToggle",
    Callback = function(Value)
        autoSwitchBlockCharge = Value
        if not Value then
            autoblocktype = originalBlockType
        else
            originalBlockType = autoblocktype
        end
    end
})

AutoBlockTab:CreateButton({
    Name = "Change auto block type",
    Callback = function()
        if autoblocktype == "Block" then
            autoblocktype = "Charge"
            originalBlockType = "Charge"
            SendNotif("Block Type", "Switched to Charge", 1.5)
        elseif autoblocktype == "Charge" then
            autoblocktype = "Block"
            originalBlockType = "Block"
            SendNotif("Block Type", "Switched to Block", 1.5)
        end
    end
})

AutoBlockTab:CreateInput({
    Name = "Block Delay (seconds)",
    PlaceholderText = "0",
    RemoveTextAfterFocusLost = false,
    Flag = "blockdelaynumber",
    Callback = function(Text)
        blockdelay = tonumber(Text) or 0
    end
})

AutoBlockTab:CreateToggle({
    Name = "Enable Facing Check",
    CurrentValue = true,
    Flag = "FacingCheckToggle",
    Callback = function(Value)
        facingCheckEnabled = Value
        if not isPlayingAsSpecialKiller then
            originalFacingCheckState = Value
        end
    end
})

AutoBlockTab:CreateInput({
    Name = "Facing Check angle (DOT)",
    PlaceholderText = "-0.3",
    RemoveTextAfterFocusLost = false,
    Flag = "Facingcheckdot",
    Callback = function(Text)
        customFacingDot = tonumber(Text) or -0.3
    end
})

AutoBlockTab:CreateInput({
    Name = "Detection Range",
    PlaceholderText = "18",
    RemoveTextAfterFocusLost = false,
    Flag = "DetectionRange",
    Callback = function(Text)
        detectionRange = tonumber(Text) or 18
    end
})

AutoBlockTab:CreateToggle({
    Name = "Range Visual",
    CurrentValue = false,
    Flag = "KillerCircleToggle",
    Callback = function(Value)
        killerCirclesVisible = Value
        if not Value then
            for _, circle in pairs(detectionCircles) do
                safeDestroyCircle(circle)
            end
            detectionCircles = {}
        end
    end
})

AutoBlockTab:CreateParagraph({
    Title = "Guest1337 Animation Types",
    Content = "• All Guest1337 animations trigger Punish features (Raging Pace/Mass Infection)\n" ..
              "• Punch animations (3 specific IDs) trigger Raging Pace Auto Parry\n\n" ..
              "Punch Animation IDs:\n" ..
              "• 87259391926321\n" ..
              "• 138040001965654\n" ..
              "• 72007882634344"
})

-- ===== TECH TAB =====
local TechTab = Window:CreateTab("Tech", 4483362458)

TechTab:CreateToggle({
    Name = "Auto Blood Orb (Sixer)",
    CurrentValue = false,
    Flag = "AutoBloodOrbToggle",
    Callback = function(Value)
        autoBloodOrb = Value
        if Value then
            startBloodOrbLoop()
            SendNotif("Auto Blood Orb", "Enabled - Automatically collects blood orbs", 2)
        else
            if bloodOrbConnection then
                bloodOrbConnection:Disconnect()
                bloodOrbConnection = nil
            end
            SendNotif("Auto Blood Orb", "Disabled", 1)
        end
    end
})

TechTab:CreateParagraph({
    Title = "Auto Blood Orb Info",
    Content = "When playing as Sixer, automatically brings all Blood Orbs to you.\n" ..
              "Won't collect if:\n" ..
              "• Blood storage is full\n" ..
              "• Timer is active (not 0 or TIME'S UP)\n" ..
              "• You are not playing as Sixer"
})

-- ===== AIMBOT TAB =====
local AimbotTab = Window:CreateTab("Aimbot", 4483362458)

local AimbotToggle = AimbotTab:CreateToggle({
    Name = "Enable Aimbot",
    CurrentValue = false,
    Flag = "AimbotToggle",
    Callback = function(Value)
        aimbotEnabled = Value
        if Value then
            updateAimbot()
            if aimbotUseFOV then
                createFOVCircle()
            end
            SendNotif("Aimbot", "Enabled", 2)
        else
            if aimbotConnection then
                aimbotConnection:Disconnect()
                aimbotConnection = nil
            end
            if aimbotFOVCircle then
                pcall(function() aimbotFOVCircle:Destroy() end)
                aimbotFOVCircle = nil
            end
            stopAimbot()
            positionHistory = {}
            SendNotif("Aimbot", "Disabled", 1)
        end
    end
})

AimbotTab:CreateKeybind({
    Name = "Toggle Aimbot (Keybind)",
    CurrentKeybind = "Z",
    HoldToInteract = false,
    Flag = "AimbotToggleKey",
    Callback = function()
        local newState = not aimbotEnabled
        AimbotToggle:Set(newState)
        SendNotif("Aimbot", newState and "Enabled" or "Disabled", 1)
    end
})

AimbotTab:CreateToggle({
    Name = "TwoTime: Face Same Side As Killer (Backstab Mode)",
    CurrentValue = false,
    Flag = "FaceKillerSideToggle",
    Callback = function(Value)
        faceSameSideAsKiller = Value
        if Value then
            SendNotif("TwoTime", "Normal stab will now ROTATE CHARACTER to face killer side (NO shiftlock)", 1)
        else
            SendNotif("TwoTime", "Normal stab will now move CAMERA to aim at killer (Shiftlock ON)", 1)
        end
    end
})

AimbotTab:CreateSlider({
    Name = "Aimbot Smoothness",
    Range = {0.01, 0.5},
    Increment = 0.01,
    Suffix = "",
    CurrentValue = 0.1,
    Flag = "AimbotSmoothness",
    Callback = function(Value)
        aimbotSmoothness = Value
    end
})

AimbotTab:CreateSlider({
    Name = "Prediction Distance Multiplier",
    Range = {1, 3},
    Increment = 0.1,
    Suffix = "x",
    CurrentValue = 1,
    Flag = "PredictionMultiplier",
    Callback = function(Value)
        aimbotPredictionMultiplier = Value
        SendNotif("Prediction", "Multiplier set to " .. Value .. "x", 1)
    end
})

AimbotTab:CreateSlider({
    Name = "Max Distance",
    Range = {10, 200},
    Increment = 5,
    Suffix = "studs",
    CurrentValue = 50,
    Flag = "AimbotMaxDistance",
    Callback = function(Value)
        aimbotMaxDistance = Value
    end
})

local FOVModeToggle = AimbotTab:CreateToggle({
    Name = "FOV Mode (ON) / Nearest Mode (OFF)",
    CurrentValue = true,
    Flag = "FOVModeToggle",
    Callback = function(Value)
        aimbotUseFOV = Value
        if aimbotEnabled then
            if Value then
                createFOVCircle()
                SendNotif("Target Mode", "Using FOV circle for target selection", 2)
            else
                if aimbotFOVCircle then
                    pcall(function() aimbotFOVCircle:Destroy() end)
                    aimbotFOVCircle = nil
                end
                SendNotif("Target Mode", "Using nearest target (ignores FOV)", 2)
            end
        end
    end
})

AimbotTab:CreateSlider({
    Name = "Field of View Size",
    Range = {30, 360},
    Increment = 5,
    Suffix = "degrees",
    CurrentValue = 90,
    Flag = "AimbotFOV",
    Callback = function(Value)
        aimbotFOV = Value
        if aimbotEnabled and aimbotUseFOV then
            createFOVCircle()
        end
    end
})

AimbotTab:CreateParagraph({
    Title = "Aimbot Features",
    Content = "• LEFT CLICK:\n" ..
              "  - Silent M1 cooldown check for ALL killers (0, 0.0, 0.1 = ready)\n" ..
              "  - Sixer: 2s if Carving Slash has charges, 0.8s if no charges\n" ..
              "  - Other Killers: 0.65s\n" ..
              "• Q ABILITY:\n" ..
              "  - Noli: 2.5s\n" ..
              "  - Sixer: 1.45s\n" ..
              "  - Slasher: 0.95s\n" ..
              "  - JohnDoe: 4.5s\n" ..
              "  - 1x1x1x1: 2.1s\n" ..
              "  - Shedletsky: 1.1s\n" ..
              "  - TwoTime: 0.4s (with cooldown check & crouch dash stab!)\n" ..
              "  - Elliot: 0.5s\n" ..
              "• E ABILITY:\n" ..
              "  - c00lkidd: 0.6s\n" ..
              "  - Sixer: 1.0s\n" ..
              "  - Chance: 1.1s\n" ..
              "  - 1x1x1x1: 1.2s\n" ..
              "  - Slasher: 0.5s\n" ..
              "  - Jane Doe: 0.9s\n" ..
              "• R ABILITY:\n" ..
              "  - Guest1337: 0.95s\n" ..
              "• Press Z to toggle Aimbot on/off\n" ..
              "• PREDICTION: Red sphere appears when targeting\n" ..
              "• TwoTime: Crouch + Q = Dash Stab (0.35s dash, 0.4s backstab)\n" ..
              "• Sixer: Blood Hunt Auto Parry (presses R) available in Auto Block tab"
})

local CharacterInfoButton = AimbotTab:CreateButton({
    Name = "Show My Character Info",
    Callback = function()
        local team, character = getMyCharacterInfo()
        local info = string.format("Team: %s\nCharacter: %s", 
            tostring(team), 
            tostring(character))
        SendNotif("Character Info", info, 5)
    end
})

-- ===== VISUALS TAB =====
local VisualsTab = Window:CreateTab("Visuals", 4483362458)

VisualsTab:CreateToggle({
    Name = "Show Stun Immune Indicator",
    CurrentValue = false,
    Flag = "StunImmuneToggle",
    Callback = function(Value)
        stunImmuneEnabled = Value
        if Value then
            if KillersFolder then
                for _, killer in ipairs(KillersFolder:GetChildren()) do
                    local hrp = killer:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        local billboard = hrp:FindFirstChild("StunImmuneBillboard")
                        if billboard then
                            billboard:Destroy()
                        end
                    end
                end
            end
            startStunImmuneLoop()
            setupStunImmuneListeners()
            SendNotif("Stun Immune", "Enabled - Shows ⚡ STUN IMMUNE ⚡ above stunned killers", 2)
        else
            if stunImmuneLoop then
                task.cancel(stunImmuneLoop)
                stunImmuneLoop = nil
            end
            if KillersFolder then
                for _, killer in ipairs(KillersFolder:GetChildren()) do
                    local hrp = killer:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        local billboard = hrp:FindFirstChild("StunImmuneBillboard")
                        if billboard then
                            billboard:Destroy()
                        end
                    end
                end
            end
            SendNotif("Stun Immune", "Disabled", 1)
        end
    end
})

-- Ability Cooldown Tracker Toggle
local CooldownTrackerToggle = VisualsTab:CreateToggle({
    Name = "Show Ability Cooldowns",
    CurrentValue = false,
    Flag = "ShowCooldowns",
    Callback = function(Value)
        cooldownTrackerEnabled = Value
        
        if Value then
            scanForAllCooldownSurvivors()
            watchForNewCooldownSurvivors()
            
            task.spawn(function()
                while cooldownTrackerEnabled do
                    task.wait(5)
                    scanForAllCooldownSurvivors()
                end
            end)
            
            SendNotif("Cooldown Tracker", "Enabled - Tracking survivor ability cooldowns", 2)
        else
            for survivor, data in pairs(activeCooldownTimers) do
                if data.billboard then
                    pcall(function() data.billboard:Destroy() end)
                end
                if data.connection then
                    data.connection:Disconnect()
                end
                if survivor then
                    survivor:SetAttribute("CooldownListener", nil)
                end
            end
            activeCooldownTimers = {}
            lastGuest1337Trigger = {}
            lastChanceTrigger = {}
            SendNotif("Cooldown Tracker", "Disabled", 1)
        end
    end
})

VisualsTab:CreateParagraph({
    Title = "Ability Cooldown Tracker Info",
    Content = "Shows a timer above survivors when they use their abilities.\n\n" ..
              "• Shedletsky (Sword): 40 second cooldown\n" ..
              "• TwoTime (Dagger): 30 second cooldown\n" ..
              "• Chance (Gun): 45 second cooldown (3s anti-spam)\n" ..
              "• JaneDoe (Hatchet): 35 second cooldown\n" ..
              "• Guest1337 (Block): 27 second cooldown (3s anti-spam)\n" ..
              "• Elliot (Pizza): 45 second cooldown\n\n" ..
              "• Does NOT show timers on your own character\n" ..
              "• Anti-spam prevents double-triggering from lag\n" ..
              "• Last 10 seconds show decimals (9.9, 9.8, etc.)\n" ..
              "• Timer disappears at 0.1\n" ..
              "• Color changes:\n" ..
              "  - White: >10 seconds remaining\n" ..
              "  - Orange: 5-10 seconds remaining\n" ..
              "  - Red: <5 seconds remaining"
})

-- ===== DUSEKKAR SILENT AIM TAB =====
local DusekkarTab = Window:CreateTab("Dusekkar", 4483362458)

local DusekkarToggle = DusekkarTab:CreateToggle({
    Name = "Enable Dusekkar Silent Aim",
    CurrentValue = false,
    Flag = "DusekkarToggle",
    Callback = function(Value)
        dusekkarEnabled = Value
        if Value then
            if amIDusekkar() then
                SendNotif("Dusekkar", "Enabled - Press F to fire Plasma Beam", 2)
            end
        else
            restoreSilentAim()
            silentAimActive = false
            currentDusekkarTarget = nil
            stopDusekkar()
            SendNotif("Dusekkar", "Disabled", 1)
        end
    end
})

DusekkarTab:CreateDropdown({
    Name = "Target Mode",
    Options = {"Survivors", "Killers"},
    CurrentOption = "Survivors",
    Flag = "DusekkarTargetMode",
    Callback = function(Option)
        if not amIDusekkar() then
            return
        end
        dusekkarTargetMode = Option
        SendNotif("Dusekkar", "Now targeting: " .. Option, 1)
    end
})

DusekkarTab:CreateSlider({
    Name = "Prediction Multiplier",
    Range = {1, 3},
    Increment = 0.1,
    Suffix = "x",
    CurrentValue = 1.0,
    Flag = "DusekkarPrediction",
    Callback = function(Value)
        dusekkarPredictionMultiplier = Value
        SendNotif("Dusekkar", "Prediction set to " .. Value .. "x", 1)
    end
})

DusekkarTab:CreateKeybind({
    Name = "Switch Target Mode",
    CurrentKeybind = "G",
    HoldToInteract = false,
    Flag = "DusekkarSwitchKey",
    Callback = function()
        if not amIDusekkar() then
            return
        end
        
        if dusekkarTargetMode == "Survivors" then
            dusekkarTargetMode = "Killers"
        else
            dusekkarTargetMode = "Survivors"
        end
        
        SendNotif("Dusekkar", "Now targeting: " .. dusekkarTargetMode, 2)
    end
})

DusekkarTab:CreateKeybind({
    Name = "Fire Plasma Beam (Silent)",
    CurrentKeybind = "F",
    HoldToInteract = false,
    Flag = "DusekkarFireKey",
    Callback = function()
        if not dusekkarEnabled then
            return
        end
        
        if not amIDusekkar() then
            return
        end
        
        useSilentAim()
    end
})

DusekkarTab:CreateParagraph({
    Title = "Dusekkar Silent Aim Features",
    Content = "• PURE SILENT AIM: NO camera movement, NO shiftlock\n" ..
              "• Blue prediction sphere appears when firing\n" ..
              "• TARGETS THE BLUE SPHERE (predicted position)\n" ..
              "• Prediction uses multiplier slider\n" ..
              "• Sphere stays for full 2.5 seconds (plasma beam duration)\n" ..
              "• SURVIVOR MODE: Targets survivor closest to cursor\n" ..
              "• KILLER MODE: Targets nearest killer\n" ..
              "• Press G to switch modes\n" ..
              "• Press F to fire at target\n\n" ..
              "NOTE: The Plasma Beam will aim EXACTLY where the blue sphere is!\n" ..
              "This works by hooking the game's GetMousePosition function."
})

-- ===== INITIALIZATION =====
local function setupCharacterListeners()
    if SurvivorsFolder then
        SurvivorsFolder.ChildAdded:Connect(function(newSurvivor)
            local root = newSurvivor:WaitForChild("HumanoidRootPart", 5)
            if root then
                updatePositionHistory(newSurvivor, root.Position)
            end
        end)
    end
    
    if KillersFolder then
        KillersFolder.ChildAdded:Connect(function(newKiller)
            local root = newKiller:WaitForChild("HumanoidRootPart", 5)
            if root then
                updatePositionHistory(newKiller, root.Position)
            end
        end)
    end
    
    local function watchCharacterRemoval(folder)
        if folder then
            folder.ChildRemoved:Connect(function(character)
                if positionHistory[character] then
                    positionHistory[character] = nil
                end
                if aimbotCurrentTarget == character then
                    stopAimbot()
                end
                if currentDusekkarTarget == character then
                    stopDusekkar()
                end
                if activeCooldownTimers[character] then
                    if activeCooldownTimers[character].billboard then
                        pcall(function() activeCooldownTimers[character].billboard:Destroy() end)
                    end
                    if activeCooldownTimers[character].connection then
                        activeCooldownTimers[character].connection:Disconnect()
                    end
                    activeCooldownTimers[character] = nil
                end
            end)
        end
    end
    
    watchCharacterRemoval(KillersFolder)
    watchCharacterRemoval(SurvivorsFolder)
end

setupCharacterListeners()
task.spawn(setupInputHook)
setupSilentAim()
refreshUIRefs()
Rayfield:LoadConfiguration()

print("========================================")
print("Pillager Cheats - Fully Loaded!")
print("Tabs: Auto Block, Tech, Aimbot, Visuals, Dusekkar")
print("========================================")
print("Auto Block: Guest1337, 7n7, Slasher, JohnDoe, 1x1x1x1, Sixer")
print("Aimbot: All killers and survivors with prediction")
print("Visuals: Stun Immune Indicator + Ability Cooldown Tracker")
print("Dusekkar: Silent Aim for Plasma Beam (Press F to fire)")
print("========================================")
